\section{Proofs of Proposition~\ref{prop:signflip} and Theorem~\ref{thm:monotone}}
\label{app:proof}

We provide algebraic proofs of sign preservation for the confounding and collider topologies (Proposition~\ref{prop:signflip}(a)--(b)), an explicit counterexample for exposure (Proposition~\ref{prop:signflip}(c)), numerical verification for mediation, M-bias, and front-door (Numerical Verification~1), and the monotonicity proof for confounding (Theorem~\ref{thm:monotone}).
Throughout, $A\in\{0,1,\dots,K{-}1\}$ with prior $p_k=P(A{=}k)$, and $C$ is a $K{\times}K$ column-stochastic confusion matrix with $C_{jk}=P(A^*{=}j\mid A{=}k)$.
We use category~0 as the reference and let $D_j=\mathbf{1}(A{=}j)$, $D_j^*=\mathbf{1}(A^*{=}j)$ for $j=1,\dots,K{-}1$.

\subsection{Notation and Key Identities}
\label{app:notation}

Under non-differential misclassification, the misclassified moments factor linearly through $C$.
Define the population vectors:
\begin{align}
  q_j &= P(A^*{=}j) = \textstyle\sum_k C_{jk}\,p_k, \label{eq:q}\\
  \mathrm{ct}_j &= \mathbb{E}[T\cdot D_j^*] = \textstyle\sum_k C_{jk}\,\mathbb{E}[T\cdot D_k], \label{eq:ct}\\
  \mathrm{cy}_j &= \mathbb{E}[Y\cdot D_j^*] = \textstyle\sum_k C_{jk}\,\mathbb{E}[Y\cdot D_k]. \label{eq:cy}
\end{align}
Write $q = Cp$, $\mathrm{ct} = C\,\mathbb{E}[T\cdot D]$, and $\mathrm{cy} = C\,\mathbb{E}[Y\cdot D]$, where $D=(D_0,\dots,D_{K-1})^\top$.

\subsection{Frisch--Waugh--Lovell Decomposition}
\label{app:fwl}

For topologies where $T$ is directly observed (confounding, mediation, collider, M-bias, front-door), the estimator regresses $Y$ on $Z^*=(1,T,D_1^*,\dots,D_{K-1}^*)$.
By the Frisch--Waugh--Lovell (FWL) theorem, the coefficient on $T$ satisfies:
\begin{equation}
  \operatorname{plim}\hat{\tau}(C)
  = \frac{\mathbb{E}[\tilde{T}\,\tilde{Y}]}{\mathbb{E}[\tilde{T}^2]},
  \label{eq:fwl}
\end{equation}
where $\tilde{T}=T-\mathbb{E}[T\mid A^*]$ and $\tilde{Y}=Y-\mathbb{E}[Y\mid A^*]$ are residuals from projecting onto $(1,D_1^*,\dots,D_{K-1}^*)$.

\begin{lemma}[Bias decomposition]
\label{lem:bias_decomp}
Suppose $Y = \tau T + f(A) + \varepsilon_Y$ with $\varepsilon_Y \perp (T, A, A^*)$.
Then
\begin{equation}
  \operatorname{plim}\hat\tau(C) = \tau + \frac{R(C)}{D(C)},
  \label{eq:bias_decomp}
\end{equation}
where
\begin{align}
  R(C) &= \mathbb{E}\bigl[\operatorname{Cov}(T,\,f(A)\mid A^*)\bigr], \label{eq:R}\\
  D(C) &= \mathbb{E}\bigl[\operatorname{Var}(T\mid A^*)\bigr] > 0. \label{eq:D}
\end{align}
\end{lemma}

\begin{proof}
Expand $\tilde{Y}$ using $Y=\tau T + f(A) + \varepsilon_Y$:
\[
  \tilde{Y} = \tau\,\tilde{T} + \bigl(f(A) - \mathbb{E}[f(A)\mid A^*]\bigr) + \varepsilon_Y.
\]
Since $\tilde{T}$ is measurable w.r.t.\ $(T,A^*)$ and $\varepsilon_Y\perp(T,A,A^*)$:
\[
  \mathbb{E}[\tilde{T}\,\tilde{Y}]
  = \tau\,\mathbb{E}[\tilde{T}^2]
  + \mathbb{E}\bigl[\tilde{T}\,\bigl(f(A)-\mathbb{E}[f(A)\mid A^*]\bigr)\bigr].
\]
The numerator residual is $R(C) = \mathbb{E}[\tilde{T}(f(A)-\mathbb{E}[f(A)\mid A^*])]$, and the denominator is $D(C) = \mathbb{E}[\tilde{T}^2]$.

By the law of total covariance:
\[
  R(C) = \sum_{j=0}^{K-1} q_j\,\operatorname{Cov}(T,\,f(A)\mid A^*{=}j)
  = \mathbb{E}[\operatorname{Cov}(T,f(A)\mid A^*)].
\]
For $D(C)>0$: decompose $T = g(A) + \varepsilon_T$ where $g(A)=\mathbb{E}[T\mid A]$ (for confounding) or simply note that $T$ has exogenous variation independent of $A^*$ (for mediation).
In both cases, $\tilde{T}=\bigl(g(A)-\mathbb{E}[g(A)\mid A^*]\bigr)+\varepsilon_T'$, where $\varepsilon_T'$ is the component of $T$ independent of $(A,A^*)$.
Hence $D(C)\geq\operatorname{Var}(\varepsilon_T')>0$.
\end{proof}

\subsection{Confounding Topology ($A$ = Confounder)}
\label{app:confounding}

\paragraph{Data-generating process.}
$A\to T$, $A\to Y$, $T\to Y$.
Using dummy coding with category 0 as reference:
\begin{align*}
  T &= \sum_{j=1}^{K-1}\alpha_j D_j + \varepsilon_T,\quad
  \varepsilon_T\sim\mathcal{N}(0,\sigma_T^2),\;\varepsilon_T\perp A,\\
  Y &= \tau\, T + \sum_{j=1}^{K-1}\beta_j^{(A)} D_j + \varepsilon_Y,\quad
  \varepsilon_Y\sim\mathcal{N}(0,1),\;\varepsilon_Y\perp(T,A).
\end{align*}
Define $g(k)=\sum_{j}\alpha_j\mathbf{1}(k{=}j)$ and $f(k)=\sum_{j}\beta_j^{(A)}\mathbf{1}(k{=}j)$, so $g(0)=f(0)=0$.

\paragraph{Closed-form plim.}
Using the FWL decomposition, the coefficient on $T$ is:
\begin{equation}
  \operatorname{plim}\hat\tau(C)
  = \frac{\mu_{TY} - \sum_{j=0}^{K-1}\frac{\mathrm{ct}_j\,\mathrm{cy}_j}{q_j}}
  {s_T - \sum_{j=0}^{K-1}\frac{\mathrm{ct}_j^2}{q_j}},
  \label{eq:plim_conf}
\end{equation}
where $s_T=\mathbb{E}[T^2]$, $\mu_{TY}=\mathbb{E}[TY]$, and $\mathrm{ct}_j$, $\mathrm{cy}_j$, $q_j$ are defined in~\eqref{eq:q}--\eqref{eq:cy}.

\begin{proof}[Derivation]
By FWL, $\operatorname{plim}\hat\tau = \mathbb{E}[\tilde{T}Y]/\mathbb{E}[\tilde{T}^2]$ where $\tilde{T}=T-\mathbb{E}[T\mid A^*]$.
Since $\mathbb{E}[T\mid A^*{=}j]=\mathrm{ct}_j/q_j$:
\begin{align*}
  \mathbb{E}[\tilde{T}^2]
  &= \mathbb{E}[T^2] - \sum_j q_j\bigl(\mathrm{ct}_j/q_j\bigr)^2
  = s_T - \sum_j \mathrm{ct}_j^2/q_j,\\
  \mathbb{E}[\tilde{T}\,Y]
  &= \mathbb{E}[TY] - \sum_j \bigl(\mathrm{ct}_j/q_j\bigr)\,\mathbb{E}[Y\cdot D_j^*]
  = \mu_{TY} - \sum_j \mathrm{ct}_j\,\mathrm{cy}_j/q_j.
\end{align*}
\end{proof}

\paragraph{Verification at $C=I$.}
When $C=I$: $q_j=p_j$, $\mathrm{ct}_j=\mathbb{E}[T D_j]=\alpha_j p_j$, $\mathrm{cy}_j=\mathbb{E}[Y D_j]=p_j(\tau\alpha_j+\beta_j^{(A)})$.
Direct substitution yields numerator $= \tau\sigma_T^2$ and denominator $= \sigma_T^2$, giving $\operatorname{plim}\hat\tau = \tau$.

\paragraph{Bias structure.}
By Lemma~\ref{lem:bias_decomp}:
\begin{equation}
  \operatorname{plim}\hat\tau(C) = \tau + B(C),\quad
  B(C) = \frac{\mathbb{E}[\operatorname{Cov}(g(A),\,f(A)\mid A^*)]}{D(C)}.
  \label{eq:bias_conf}
\end{equation}
By the law of total covariance:
\begin{equation}
  \mathbb{E}[\operatorname{Cov}(g,f\mid A^*)]
  = \operatorname{Cov}(g(A),f(A))
  - \operatorname{Cov}\bigl(\mathbb{E}[g(A)\mid A^*],\;\mathbb{E}[f(A)\mid A^*]\bigr).
  \label{eq:lotc}
\end{equation}

At $C=I$: $\mathbb{E}[g\mid A^*]=g(A)$, so the between-group covariance equals $\operatorname{Cov}(g,f)$ and $B(I)=0$.

At $C=\frac{1}{K}\mathbf{1}\mathbf{1}^\top$ (uniform, $A^*\perp A$): $\mathbb{E}[g\mid A^*]$ is constant, so the between-group covariance is zero and
\begin{equation}
  B_{\mathrm{OV}} \;\equiv\; B\bigl(\tfrac{1}{K}\mathbf{1}\mathbf{1}^\top\bigr)
  = \frac{\operatorname{Cov}(g(A),\,f(A))}{\operatorname{Var}(T)},
  \label{eq:bov}
\end{equation}
which equals the classical omitted-variable bias (no control for $A$).

\paragraph{Sign preservation condition.}
Since $D(C)>0$, sign$(\operatorname{plim}\hat\tau) = $ sign$(\tau\cdot D(C) + R(C))$.
For sign preservation, it suffices that $R(C)$ does not overwhelm $\tau\cdot D(C)$.

\begin{proposition}[Confounding sign preservation --- proportional case]
\label{prop:conf_prop}
If $f(A) = \lambda\,g(A)$ for some constant $\lambda$ (i.e., the confounder effects on $T$ and $Y$ are proportional), then
\begin{equation}
  B(C) = \bigl(1-\xi(C)\bigr)\,B_{\mathrm{OV}},
  \label{eq:convex}
\end{equation}
where
\[
  \xi(C) = \frac{\operatorname{Var}(\mathbb{E}[g(A)\mid A^*])\;\sigma_T^2}
  {\operatorname{Var}(g(A))\bigl(\operatorname{Var}(g(A))-\operatorname{Var}(\mathbb{E}[g(A)\mid A^*])+\sigma_T^2\bigr)}
  \;\in\;[0,1].
\]
Consequently, $B(C)$ lies between $0$ and $B_{\mathrm{OV}}$, and $\operatorname{sign}(\operatorname{plim}\hat\tau)=\operatorname{sign}(\tau)$ whenever $\operatorname{sign}(\tau)=\operatorname{sign}(\tau+B_{\mathrm{OV}})$.
\end{proposition}

\begin{proof}
When $f=\lambda g$: the between-group covariance in~\eqref{eq:lotc} becomes $\lambda\,\operatorname{Var}(\mathbb{E}[g\mid A^*])$, and $\operatorname{Cov}(g,f)=\lambda\,\operatorname{Var}(g)$.
So $R(C)=\lambda\bigl(\operatorname{Var}(g)-\operatorname{Var}(\mathbb{E}[g\mid A^*])\bigr)=\lambda\,V_W$, where $V_W=\operatorname{Var}(g)-V_B$ with $V_B=\operatorname{Var}(\mathbb{E}[g\mid A^*])$.
Similarly, $D(C)=V_W+\sigma_T^2$.
Hence
\[
  B(C) = \frac{\lambda\,V_W}{V_W+\sigma_T^2},\qquad
  B_{\mathrm{OV}} = \frac{\lambda\,V}{V+\sigma_T^2},
\]
where $V=\operatorname{Var}(g)=V_B+V_W$.
The ratio
\[
  \frac{B(C)}{B_{\mathrm{OV}}} = \frac{V_W(V+\sigma_T^2)}{V(V_W+\sigma_T^2)} = 1 - \xi(C)
\]
is a decreasing function of $V_B\in[0,V]$.
At $V_B=0$ (no classification, $C$ uniform): $\xi=0$, $B=B_{\mathrm{OV}}$.
At $V_B=V$ (perfect classification, $C=I$): $\xi=1$, $B=0$.
Since $0\leq V_B\leq V$, we have $\xi\in[0,1]$ and $B(C)\in[0,B_{\mathrm{OV}}]$ (with the sign of $B_{\mathrm{OV}}$).
\end{proof}

\begin{proposition}[Confounding sign preservation --- general case]
\label{prop:conf_gen}
For general $g$ and $f$ (not necessarily proportional), define the worst-case bias bound
\begin{equation}
  \bar{B} \;=\; \frac{\sqrt{\operatorname{Var}(g(A))\,\operatorname{Var}(f(A))}}{\sigma_T^2}.
  \label{eq:bbar}
\end{equation}
If $|\tau| > \bar{B}$, then $\operatorname{sign}(\operatorname{plim}\hat\tau(C))=\operatorname{sign}(\tau)$ for \emph{all} column-stochastic $C$.
\end{proposition}

\begin{proof}
We need $\operatorname{sign}(\tau\cdot D(C)+R(C))=\operatorname{sign}(\tau)$, which holds if $|R(C)|<|\tau|\cdot D(C)$.
Since $D(C)\geq\sigma_T^2$ (shown in Lemma~\ref{lem:bias_decomp}), it suffices to show $|R(C)|<|\tau|\,\sigma_T^2$.

\emph{Step 1: Bound $|R(C)|$.}
$R(C)=\mathbb{E}[\operatorname{Cov}(g(A),f(A)\mid A^*)]$.
By the Cauchy--Schwarz inequality applied to each within-group covariance:
\[
  |\operatorname{Cov}(g,f\mid A^*{=}j)|
  \leq \sqrt{\operatorname{Var}(g\mid A^*{=}j)\,\operatorname{Var}(f\mid A^*{=}j)}.
\]
Taking expectations and applying Cauchy--Schwarz again:
\begin{align*}
  |R(C)|
  &\leq \mathbb{E}\bigl[\sqrt{\operatorname{Var}(g\mid A^*)\,\operatorname{Var}(f\mid A^*)}\bigr]\\
  &\leq \sqrt{\mathbb{E}[\operatorname{Var}(g\mid A^*)]\;\mathbb{E}[\operatorname{Var}(f\mid A^*)]}\\
  &\leq \sqrt{\operatorname{Var}(g(A))\;\operatorname{Var}(f(A))},
\end{align*}
where the last step uses $\mathbb{E}[\operatorname{Var}(X\mid Y)]\leq\operatorname{Var}(X)$ (law of total variance).

\emph{Step 2: Conclude.}
The bias satisfies
\[
  |B(C)| = \frac{|R(C)|}{D(C)} \leq \frac{\sqrt{\operatorname{Var}(g)\,\operatorname{Var}(f)}}{\sigma_T^2} = \bar{B}.
\]
When $|\tau|>\bar{B}$, the bias cannot cancel $\tau$, so $\operatorname{sign}(\operatorname{plim}\hat\tau)=\operatorname{sign}(\tau)$ for all $C$.
\end{proof}

\paragraph{Relationship to the omitted-variable condition.}
A weaker (necessary) condition for sign preservation is $\operatorname{sign}(\tau)=\operatorname{sign}(\tau_{\mathrm{OV}})$ where $\tau_{\mathrm{OV}}=\tau+\operatorname{Cov}(g,f)/\operatorname{Var}(T)$.
If this fails, then $C=\frac{1}{K}\mathbf{1}\mathbf{1}^\top$ (uniform noise) already flips the sign.
The rigorous sufficient condition $|\tau|>\bar{B}$ implies the weaker condition, since $|B_{\mathrm{OV}}|=|\operatorname{Cov}(g,f)|/\operatorname{Var}(T)\leq\bar{B}$ by Cauchy--Schwarz and $\operatorname{Var}(T)\geq\sigma_T^2$.

\paragraph{Verification for paper parameters ($K{=}3$).}
With $\tau{=}2.0$, $\boldsymbol{\alpha}{=}(1.5,{-}1.0)$, $\boldsymbol{\beta}^{(A)}{=}(1.0,{-}0.5)$, $\sigma_T{=}1.0$, $\mathbf{p}{=}(0.40,0.35,0.25)$:
\begin{align*}
  \operatorname{Var}(g) &= \sum_k p_k\alpha_k^2 - \bigl(\textstyle\sum_k p_k\alpha_k\bigr)^2 = 0.962,\\
  \operatorname{Var}(f) &= \sum_k p_k(\beta_k^{(A)})^2 - \bigl(\textstyle\sum_k p_k\beta_k^{(A)}\bigr)^2 = 0.362,\\
  \bar{B} &= \sqrt{0.962\times 0.362}\;/\;1.0^2 = 0.590.
\end{align*}
Since $|\tau|{=}2.0 > \bar{B}{=}0.590$, sign preservation is guaranteed for \emph{all} column-stochastic $C$ by Proposition~\ref{prop:conf_gen}.
Additionally, $B_{\mathrm{OV}}=\operatorname{Cov}(g,f)/\operatorname{Var}(T)=0.588/1.962={+}0.300$, giving $\tau_{\mathrm{OV}}{=}2.300>0$, confirming that both the necessary and sufficient conditions are satisfied.

\paragraph{Extension to $K > 3$.}
For $K{>}3$, treatment effect coefficients are drawn from a fixed pool with alternating signs and decreasing magnitudes: $\boldsymbol{\beta} = (0, 1.0, {-}0.5, 0.7, {-}0.3, 0.5, {-}0.2, 0.4, {-}0.15, 0.3, \ldots)$, truncated to $K$ entries. The category distribution uses $p_k \propto 1/(k{+}1)$, normalized to sum to 1. Other topology-specific parameters (confounding strengths, mediator effects, etc.) follow analogous alternating-sign pools. For $K{=}10$, $\boldsymbol{\beta} = (0, 1.0, {-}0.5, 0.7, {-}0.3, 0.5, {-}0.2, 0.4, {-}0.15, 0.3)$.

\subsection{Proof of Theorem~\ref{thm:monotone} (Confounding Monotonicity)}
\label{app:monotonicity_proof}

Under the interpolation $C(\delta)=(1{-}\delta)I+\delta C_0$, we prove $|B(C(\delta))|$ is non-decreasing in $\delta$ for proportional effects ($f=\lambda g$) and provide numerical verification for the general case.
The proof proceeds by direct algebraic analysis of $V_B(\delta)=\operatorname{Var}(\mathbb{E}[g(A)\mid A^*_\delta])$ as a rational function of $\delta$, establishing convexity and bounding the derivative at both endpoints.

\paragraph{Proportional case ($f=\lambda g$).}
From Proposition~\ref{prop:conf_prop}, $B(C)=\lambda V_W/(V_W+\sigma_T^2)$ where $V_W=\operatorname{Var}(g)-V_B$ and $V_B(\delta)=\operatorname{Var}(\mathbb{E}[g(A)\mid A^*_\delta])$.

\emph{Step 1: Convexity of $V_B(\delta)$.}
Define $N_j(\delta)=\sum_k g_k\,C(\delta)_{jk}\,p_k$ and $q_j(\delta)=\sum_k C(\delta)_{jk}\,p_k$.
Both are affine in $\delta$, with $q_j(\delta)>0$.
Then $V_B(\delta)=\sum_j N_j(\delta)^2/q_j(\delta) - \bar{g}^2$.
Each term $N_j^2/q_j$ is the composition of the perspective function $(x,t)\mapsto x^2/t$ (convex for $t>0$) with the affine map $\delta\mapsto(N_j(\delta),\,q_j(\delta))$, hence convex in $\delta$.
A sum of convex functions is convex, so $V_B(\delta)$ is convex on $[0,\delta_{\max}]$.

\emph{Step 2: Derivative formula.}
Let $\mu_j(\delta)=N_j(\delta)/q_j(\delta)=\mathbb{E}[g(A)\mid A^*{=}j]$.
Then
\begin{equation}
\frac{dV_B}{d\delta}
= -\sum_{j,k}\bigl[(C_0)_{jk}-\mathbf{1}_{j=k}\bigr]\,p_k\,(g_k-\mu_j(\delta))^2.
\label{eq:dVB}
\end{equation}
\emph{Derivation.}
$\frac{d}{d\delta}\frac{N_j^2}{q_j}=2\mu_j\dot{N}_j-\mu_j^2\dot{q}_j$
where $\dot{N}_j=m_j^0-g_jp_j$ and $\dot{q}_j=q_j^0-p_j$ (with $m_j^0=\sum_k g_k(C_0)_{jk}p_k$, $q_j^0=\sum_k(C_0)_{jk}p_k$).
Substituting and using column-stochasticity ($\sum_j(C_0)_{jk}=\sum_j\mathbf{1}_{j=k}=1$) to cancel the $g_k^2$ terms yields~\eqref{eq:dVB}.

\emph{Step 3: $V_B'(0)\leq 0$ (universal, no conditions on $C_0$).}
At $\delta{=}0$: $\mu_j(0)=g_j$ (since $C(0)=I$), so $(g_k-\mu_j(0))^2=(g_k-g_j)^2$.
The diagonal terms ($j{=}k$) vanish.
Hence
\[
V_B'(0)=-\sum_{j\neq k}(C_0)_{jk}\,p_k\,(g_j-g_k)^2\;\leq\; 0,
\]
since every term is non-negative.

\emph{Step 4: $V_B'(1)\leq 0$ for $K{=}2$.}
For $K{=}2$ with $C_0=\bigl[\begin{smallmatrix}a&1{-}b\\1{-}a&b\end{smallmatrix}\bigr]$ and $a,b\geq\tfrac{1}{2}$,
set $g_0{=}0$, $g_1{=}1$ without loss of generality
(the general case scales by $(\Delta g)^2$).
At $\delta{=}1$: $q_0{=}ap_0{+}(1{-}b)p_1$, $q_1{=}(1{-}a)p_0{+}bp_1$,
$\mu_0{=}(1{-}b)p_1/q_0$, $\mu_1{=}bp_1/q_1$.
Expanding~\eqref{eq:dVB} at $\delta{=}1$ and factoring:
\begin{align*}
  V_B'(1) &= (1{-}a)\,p_0(\mu_0^2{-}\mu_1^2)+(1{-}b)\,p_1\bigl[(1{-}\mu_1)^2{-}(1{-}\mu_0)^2\bigr] \\
  &= (\mu_0{-}\mu_1)\,\bigl[(1{-}a)\,p_0(\mu_0{+}\mu_1)+(1{-}b)\,p_1(2{-}\mu_0{-}\mu_1)\bigr].
\end{align*}
The bracket is non-negative (since $0\leq\mu_j\leq 1$ and all coefficients are non-negative).
Direct calculation gives $\mu_0{-}\mu_1=p_0\,p_1(1{-}a{-}b)/(q_0\,q_1)$
(expand $(1{-}b)\,q_1-b\,q_0=p_0(1{-}a{-}b)$).
Under $a,b\geq\tfrac{1}{2}$: $a{+}b\geq 1$, so $\mu_0{-}\mu_1\leq 0$ and $V_B'(1)\leq 0$.

\emph{Step 5: Monotonicity from convexity.}
Since $V_B$ is convex (Step~1), its derivative $V_B'$ is non-decreasing.
When $V_B'(1)\leq 0$, we have $V_B'(\delta)\leq V_B'(1)\leq 0$ for all $\delta\in[0,1]$, so $V_B$ is non-increasing.

\emph{Step 6: $|B(\delta)|$ is non-decreasing.}
$V_W(\delta)=\operatorname{Var}(g)-V_B(\delta)$ is non-decreasing (Step~5).
The function $h(x)=|\lambda|\,x/(x+\sigma_T^2)$ is strictly increasing for $x\geq 0$.
Hence $|B(\delta)|=h(V_W(\delta))$ is non-decreasing. \qed

\paragraph{Condition on $C_0$.}
The proof requires $V_B'(1)\leq 0$, which is guaranteed for $K{=}2$ when $(C_0)_{jj}\geq\tfrac{1}{2}$ (Step~4).
For $K\geq 3$, $V_B'(1)\leq 0$ holds for all $g$ and $p$ if and only if a certain matrix $M-W$ (depending on $C_0$ and $p$) is negative semi-definite; simple diagonal thresholds are sufficient in practice but not universal.
Numerical verification across 20{,}000 random $(C_0,p,g)$ triples per $K\in\{3,5,7\}$ with $(C_0)_{jj}\geq 1/K$ finds worst-case $|B|$ step deviations $< 0.013$ (Table~\ref{tab:conjecture_verify}), confirming $\varepsilon$-approximate monotonicity.
A $K{=}2$ counterexample ($C_0$ the permutation matrix, $(C_0)_{jj}{=}0$) shows the diagonal condition is necessary: $V_B(\delta)=(\delta{-}\tfrac{1}{2})^2$ increases on $(\tfrac{1}{2},1]$.

\paragraph{General case (non-proportional effects).}
$|B(C(\delta))|\leq\bar{B}(\delta)$ where $\bar{B}(\delta)=\sqrt{\mathbb{E}[\operatorname{Var}(g\mid A^*_\delta)]\,\mathbb{E}[\operatorname{Var}(f\mid A^*_\delta)]}\,/\,D(\delta)$ (Proposition~\ref{prop:conf_gen}).
By the same convexity argument, each $\mathbb{E}[\operatorname{Var}(\cdot\mid A^*_\delta)]$ is non-decreasing in $\delta$, and $D(\delta)=\mathbb{E}[\operatorname{Var}(T\mid A^*_\delta)]$ is non-decreasing.
Extended verification (20{,}000 random $C_0$ matrices, $K \in \{3,5,7\}$) reveals worst-case step deviations of 0.011 (confounding), confirming approximate tightness; a strict monotonicity proof for the general case remains open.

\subsection{Mediation Topology ($A$ = Mediator)}
\label{app:mediation}

\paragraph{Data-generating process.}
$T\to A\to Y$, $T\to Y$.
\begin{align*}
  T &\sim \operatorname{Bernoulli}(0.5),\\
  A\mid T &\sim \operatorname{Categorical}\bigl(\operatorname{softmax}(\boldsymbol{\gamma}+\boldsymbol{\delta}\,T)\bigr),\\
  Y &= \tau_{\mathrm{dir}}\,T + f(A) + \varepsilon_Y.
\end{align*}

\paragraph{Bias decomposition.}
By Lemma~\ref{lem:bias_decomp}, the direct-effect estimator satisfies:
\begin{equation}
  \operatorname{plim}\hat\tau_{\mathrm{dir}}(C) = \tau_{\mathrm{dir}} + \frac{\mathbb{E}[\operatorname{Cov}(T,\,f(A)\mid A^*)]}{D(C)}.
  \label{eq:med_bias}
\end{equation}

The key structural difference from confounding: $T$ \emph{causes} $A$, so the residual covariance $\operatorname{Cov}(T,f(A)\mid A^*)$ captures the indirect effect of $T$ on $Y$ through $A$ that is not absorbed by conditioning on $A^*$.

\paragraph{Endpoint analysis.}

At $C=I$ ($A^*{=}A$): conditioning on $A$ blocks the mediated path $T\to A\to Y$, so $\operatorname{Cov}(T,f(A)\mid A)=0$ and $\operatorname{plim}\hat\tau=\tau_{\mathrm{dir}}$.

At $C=\frac{1}{K}\mathbf{1}\mathbf{1}^\top$ ($A^*\perp A$): no information about $A$ is used, and
\begin{align*}
  \mathbb{E}[\operatorname{Cov}(T,f(A)\mid A^*)] &= \operatorname{Cov}(T,f(A)),\\
  D(C) &= \operatorname{Var}(T) = 0.25.
\end{align*}
The indirect effect is $\tau_{\mathrm{ind}}=\mathbb{E}[f(A)\mid T{=}1]-\mathbb{E}[f(A)\mid T{=}0]$, and
\[
  \operatorname{Cov}(T,f(A)) = 0.25\,\tau_{\mathrm{ind}}.
\]
Hence at $C$ uniform: $\operatorname{plim}\hat\tau = \tau_{\mathrm{dir}}+\tau_{\mathrm{ind}} = \tau_{\mathrm{total}}$.

\paragraph{Numerical Verification (Mediation sign preservation).}
\label{find:med_sign}
$\operatorname{sign}(\operatorname{plim}\hat\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{dir}})$ whenever $\operatorname{sign}(\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{total}})$, where $\tau_{\mathrm{total}}=\tau_{\mathrm{dir}}+\tau_{\mathrm{ind}}$.
This result is verified numerically for $100{,}000$ independently sampled column-stochastic matrices $C$; a closed-form proof for general $K$ remains open.

\begin{proof}
Let $R(C)=\mathbb{E}[\operatorname{Cov}(T,f(A)\mid A^*)]$ and $D(C)=\mathbb{E}[\operatorname{Var}(T\mid A^*)]>0$.

\emph{Endpoints.}
At $C{=}I$: $f(A)$ is constant given $A{=}A^*$, so $R(I)=0$ and $B(I)=0$.
At $C=\frac{1}{K}\mathbf{1}\mathbf{1}^\top$: $R=\operatorname{Cov}(T,f(A))=0.25\,\tau_{\mathrm{ind}}$ and $D=\operatorname{Var}(T)=0.25$, giving $B=\tau_{\mathrm{ind}}$.

\emph{Within-group structure for binary $T$.}
Since $T\in\{0,1\}$, the within-$A^*$-group bias admits a revealing decomposition.
Let $p_j=P(T{=}1\mid A^*{=}j)$.
Then $\operatorname{Var}(T\mid A^*{=}j)=p_j(1{-}p_j)$ and
\[
  \operatorname{Cov}(T,f(A)\mid A^*{=}j)
  = p_j(1{-}p_j)\bigl[\mathbb{E}[f(A)\mid T{=}1,A^*{=}j]-\mathbb{E}[f(A)\mid T{=}0,A^*{=}j]\bigr].
\]
Define $\delta_j=\mathbb{E}[f(A)\mid T{=}1,A^*{=}j]-\mathbb{E}[f(A)\mid T{=}0,A^*{=}j]$ as the \emph{conditional indirect effect} in the $A^*{=}j$ subpopulation.
Then
\[
  B(C)=\frac{R(C)}{D(C)}
  =\frac{\sum_j q_j\,p_j(1{-}p_j)\,\delta_j}{\sum_j q_j\,p_j(1{-}p_j)}
  =\sum_j w_j\,\delta_j,
\]
a weighted average of the conditional indirect effects, with weights $w_j=q_j p_j(1{-}p_j)/\sum_\ell q_\ell p_\ell(1{-}p_\ell)\geq 0$ summing to 1.

At $C{=}I$: each $A^*{=}j$ group has $A{=}j$ a.s., so $\delta_j=f(j)-f(j)=0$ and $B=0$.
At $C$ uniform: $\delta_j=\tau_{\mathrm{ind}}$ for all $j$ and $B=\tau_{\mathrm{ind}}$.

\emph{Sign preservation.}
When $\operatorname{Cov}(g(A),f(A))>0$ (the indirect effect reinforces the direct effect, i.e., $\tau_{\mathrm{dir}}$ and $\tau_{\mathrm{ind}}$ have the same sign), the bias $B(C)\geq 0$ cannot oppose $\tau_{\mathrm{dir}}$, so sign is preserved trivially.

When $\tau_{\mathrm{dir}}$ and $\tau_{\mathrm{ind}}$ have opposite signs but $|\tau_{\mathrm{ind}}|<|\tau_{\mathrm{dir}}|$ (i.e., $\operatorname{sign}(\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{total}})$), sign reversal requires some $|\delta_j|>|\tau_{\mathrm{dir}}|$ with the opposing sign.
Since each $\delta_j$ is a difference of weighted averages of $f$ values (with weights $P(A{=}k\mid T{=}t,A^*{=}j)$), and $B(C)$ is a convex combination of $\delta_j$, the bound $|B(C)|\leq\max_j|\delta_j|$ holds.
We verify numerically that $\max_j|\delta_j|\leq|\tau_{\mathrm{ind}}|$ for all 100{,}000 random $C$ matrices in our experiments, confirming sign preservation.
\end{proof}

\paragraph{Verification for paper parameters ($K{=}3$).}
With $\tau_{\mathrm{dir}}{=}0.5$, $\boldsymbol{\gamma}{=}(0,0,0)$, $\boldsymbol{\delta}{=}(0,0.8,{-}0.6)$, $\boldsymbol{\beta}{=}(0,1.0,{-}0.5)$:

$\tau_{\mathrm{ind}}=\mathbb{E}[f(A)\mid T{=}1]-\mathbb{E}[f(A)\mid T{=}0]=0.351$ (computed from the softmax class probabilities).
$\tau_{\mathrm{total}}=0.5+0.351=0.851>0$.
Since $\tau_{\mathrm{dir}}{>}0$ and $\tau_{\mathrm{total}}{>}0$, sign preservation holds for all $C$.

\paragraph{Mediation monotonicity (proportional effects).}
\label{par:med_monotone}

Under the interpolation $C(\delta)=(1{-}\delta)I+\delta C_0$, we prove $|B(C(\delta))|$ is non-decreasing in $\delta$ when the mediator's effect on $Y$ is proportional to its informativeness about $T$: $f(A)=\lambda\,g(A)$ for some scalar $\lambda$, where $g(k)=\mathbb{E}[T\mid A{=}k]$.

\emph{Step 1: Decompose $T$ via the mediator.}
Define $g(A)=\mathbb{E}[T\mid A]$ and $\varepsilon_T = T - g(A)$.
By construction, $\mathbb{E}[\varepsilon_T\mid A]=0$.
By non-differential misclassification ($T\perp A^*\mid A$):
$\mathbb{E}[\varepsilon_T\mid A{=}k, A^*{=}j] = 0$,
so $\mathbb{E}[\varepsilon_T\mid A^*{=}j]=0$ and $\operatorname{Cov}(\varepsilon_T,\,g(A)\mid A^*{=}j)=0$ for every $j$.

\emph{Step 2: Reduce the numerator.}
Since $f=\lambda g$:
\begin{align*}
  R(C) &= \lambda\,\mathbb{E}[\operatorname{Var}(g(A)\mid A^*)] = \lambda\,V_W.
\end{align*}

\emph{Step 3: Constant additive term in denominator.}
\begin{align*}
  D(C) &= \mathbb{E}[\operatorname{Var}(T\mid A^*)]
       = V_W + \sigma_\varepsilon^2,
\end{align*}
where $\sigma_\varepsilon^2\equiv\mathbb{E}[\operatorname{Var}(\varepsilon_T\mid A^*)]$.
This is \emph{constant} (independent of $C$):
$\mathbb{E}[\operatorname{Var}(\varepsilon_T\mid A^*)]
  = \sum_k p_k\,\operatorname{Var}(T\mid A{=}k)
  = \mathbb{E}[\operatorname{Var}(T\mid A)]$,
where the equality uses column-stochasticity: $\sum_j q_j\,P(A{=}k\mid A^*{=}j)=p_k$.

\emph{Step 4: Closed form and monotonicity.}
$B(C) = \lambda\,V_W/(V_W+\sigma_\varepsilon^2)$,
structurally identical to confounding (Proposition~\ref{prop:conf_prop}) with $\sigma_\varepsilon^2$ playing the role of $\sigma_T^2$.
Since $V_B(\delta)$ is convex with $V_B'(0)\leq 0$ (same argument as \S\ref{app:monotonicity_proof}, Steps~1--3), $V_W(\delta)=V{-}V_B(\delta)$ is non-decreasing under the same conditions on $C_0$.
Since $h(x)=|\lambda|x/(x+\sigma_\varepsilon^2)$ is increasing, $|B(C(\delta))|$ is non-decreasing.

\paragraph{Boundedness of the leakage coefficient.}

The mediation bias~\eqref{eq:mediation} writes the residual indirect effect as $(1-\lambda(C))\,\tau_{\mathrm{ind}}$, which (for $\tau_{\mathrm{ind}}\neq 0$) \emph{implicitly} defines
\begin{equation}
\label{eq:lambda_def}
  \lambda(C) \;:=\; 1-\frac{B(C)}{\tau_{\mathrm{ind}}},\qquad
  B(C)\;\equiv\;\operatorname{plim}\hat{\tau}_{\mathrm{dir}}(A^*)-\tau_{\mathrm{dir}},\quad
  \tau_{\mathrm{ind}}\;=\;\frac{\operatorname{Cov}(T,f(A))}{\operatorname{Var}(T)}.
\end{equation}
Substituting back into~\eqref{eq:mediation} gives the convex-combination form
$\operatorname{plim}\hat\tau_{\mathrm{dir}}(C)=\lambda(C)\,\tau_{\mathrm{dir}}+(1-\lambda(C))\,\tau_{\mathrm{total}}$, so $\lambda(C)$ is the weight placing the biased plim between $\tau_{\mathrm{dir}}$ (perfect classification) and $\tau_{\mathrm{total}}$ (no information).

\begin{proposition}[Mediation leakage boundedness]
\label{prop:lambda_bound}
Let $\lambda(C)$ be defined by~\eqref{eq:lambda_def} with $\tau_{\mathrm{ind}}\neq 0$, and let $C$ be column-stochastic.
\begin{enumerate}
\item[(a)] $\lambda(C)\leq 1$ iff $\operatorname{sign}(B(C))=\operatorname{sign}(\tau_{\mathrm{ind}})$ (the bias points in the direction of the indirect effect; no overcorrection).
\item[(b)] $\lambda(C)\geq 0$ iff additionally $|B(C)|\leq|\tau_{\mathrm{ind}}|$ (the bias does not exceed the indirect effect in magnitude).
\end{enumerate}
Equivalently, $\lambda(C)\in[0,1]$ iff $\operatorname{plim}\hat\tau_{\mathrm{dir}}(C)\in[\min(\tau_{\mathrm{dir}},\tau_{\mathrm{total}}),\,\max(\tau_{\mathrm{dir}},\tau_{\mathrm{total}})]$.

Under \emph{proportional effects} ($f(A)=\lambda_0\,g(A)$ with $g(k)=\mathbb{E}[T\mid A{=}k]$), both (a) and (b) hold for every $C$, so $\lambda(C)\in[0,1]$ unconditionally.
For \emph{general (non-proportional)} $f$, both (a) and (b) can fail because $\operatorname{Cov}(\mathbb{E}[g\mid A^*],\mathbb{E}[f\mid A^*])$ may differ in sign or magnitude from $\operatorname{Cov}(g,f)$ (a covariance-level analogue of Simpson's paradox under $A^*$ aggregation); the numerical verification below quantifies the empirical rate.
The endpoints $\lambda(I)=1$ and $\lambda(C)\to 0$ in the rank-one (uninformative) limit hold in all regimes.

\emph{Sign preservation.} When $\lambda(C)\in[0,1]$, the convex-combination form gives $\operatorname{plim}\hat\tau_{\mathrm{dir}}(C)\in[\min(\tau_{\mathrm{dir}},\tau_{\mathrm{total}}),\,\max(\tau_{\mathrm{dir}},\tau_{\mathrm{total}})]$. If additionally $\operatorname{sign}(\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{total}})$, both endpoints share the same sign, so $\operatorname{sign}(\operatorname{plim}\hat\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{dir}})$ for every~$C$. Under proportional effects, $\lambda(C)\in[0,1]$ is automatic (above), so sign preservation of $\hat\tau_{\mathrm{dir}}$ reduces to the single condition $\operatorname{sign}(\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{total}})$.
\end{proposition}

\begin{proof}
\emph{(a),(b).} With $\tau_{\mathrm{ind}}\neq 0$, $\lambda(C)\leq 1\iff B(C)/\tau_{\mathrm{ind}}\geq 0\iff \operatorname{sign}(B(C))=\operatorname{sign}(\tau_{\mathrm{ind}})$. Given (a), $\lambda(C)\geq 0\iff B(C)/\tau_{\mathrm{ind}}\leq 1\iff|B(C)|\leq|\tau_{\mathrm{ind}}|$.

\emph{Proportional case.} Step~4 of the mediation derivation gives $B(C)=\lambda_0\,V_W(C)/(V_W(C)+\sigma_\varepsilon^2)$; the rank-one limit $V_W{\to}V$ yields $\tau_{\mathrm{ind}}=\lambda_0\,V/(V+\sigma_\varepsilon^2)$. Both quantities carry $\operatorname{sign}(\lambda_0)$, establishing~(a). The ratio
\[
  \frac{B(C)}{\tau_{\mathrm{ind}}}\;=\;\frac{V_W(C)\,(V+\sigma_\varepsilon^2)}{V\,(V_W(C)+\sigma_\varepsilon^2)}
\]
is monotone increasing in $V_W(C)\in[0,V]$ with range $[0,1]$ (its derivative in $V_W$ is $\sigma_\varepsilon^2(V+\sigma_\varepsilon^2)/[V(V_W+\sigma_\varepsilon^2)^2]>0$), establishing~(b). Hence $\lambda(C)\in[0,1]$ unconditionally.

\emph{General case.} Substituting $\lambda(C)$ from~\eqref{eq:lambda_def} into~\eqref{eq:mediation} yields $\operatorname{plim}\hat\tau_{\mathrm{dir}}(C)=\lambda(C)\,\tau_{\mathrm{dir}}+(1{-}\lambda(C))\,\tau_{\mathrm{total}}$, so $\lambda(C)\in[0,1]$ iff $\operatorname{plim}\hat\tau_{\mathrm{dir}}(C)$ lies in the closed interval with endpoints $\tau_{\mathrm{dir}}$ and $\tau_{\mathrm{total}}$.

\emph{Endpoints.} At $C{=}I$, $A^*{=}A$ a.s., $\mathbb{E}[T\mid A^*]=g(A)$, and FWL gives $\operatorname{plim}\hat\tau_{\mathrm{dir}}=\tau_{\mathrm{dir}}$, hence $B(I)=0$ and $\lambda(I)=1$. At any rank-one $C$ (rows identical), $A^*\perp A$ so $\mathbb{E}[T\mid A^*]$ is constant, the partial regression on $A^*$ has no effect, and $\operatorname{plim}\hat\tau_{\mathrm{dir}}=\operatorname{Cov}(Y,T)/\operatorname{Var}(T)=\tau_{\mathrm{total}}$, giving $B=\tau_{\mathrm{ind}}$ and $\lambda=0$.
\end{proof}

\paragraph{Relation to the between-variance ratio.}
A natural \emph{diagnostic} closely related to $\lambda(C)$ is the between-variance ratio
\begin{equation}
\label{eq:vb_ratio}
  \lambda_V(C) \;:=\; \frac{V_B(C)}{V},\qquad V_B(C)\;=\;\operatorname{Var}\!\bigl(\mathbb{E}[g(A)\mid A^*]\bigr),\quad V\;=\;\operatorname{Var}(g(A)),
\end{equation}
which satisfies $\lambda_V(C)\in[0,1]$ \emph{unconditionally} by the law of total variance applied to $g(A)$. Under proportional effects, $\lambda$ and $\lambda_V$ agree at the endpoints and are related by
\[
  1-\lambda(C)\;=\;\frac{V_W(C)\,(V+\sigma_\varepsilon^2)}{V\,(V_W(C)+\sigma_\varepsilon^2)}\;=\;\bigl(1-\lambda_V(C)\bigr)\cdot\frac{V+\sigma_\varepsilon^2}{V_W(C)+\sigma_\varepsilon^2},
\]
so they coincide in the limit $\sigma_\varepsilon^2\gg V$. They generally differ for non-proportional $f$. We use $\lambda$ throughout the main text because it parameterizes~\eqref{eq:mediation} directly; $\lambda_V$ remains a useful summary of the channel quality of $A^*$ as a proxy for $A$ (independent of the structural form of $f$).

\paragraph{Numerical verification.}
For each $K\in\{3,5,7\}$ we draw $N{=}10{,}000$ configurations $(\pi,g,\sigma_\varepsilon^2,C)$ with $\pi\sim\operatorname{Dirichlet}(\mathbf{1})$, $g\sim\mathcal{N}(0,I_K)$, $\sigma_\varepsilon^2\sim\operatorname{Uniform}(0.1,2)$, columns of $C\sim\operatorname{Dirichlet}(\mathbf{1})$, and evaluate $\lambda(C)$ from~\eqref{eq:lambda_def} in two regimes for $f$:
\begin{itemize}\itemsep0pt
\item \textbf{Proportional} ($f=\lambda_0\,g$, $\lambda_0\sim\mathcal{N}(0,1)\!\setminus\!\{0\}$): $\lambda(C)\in[0,1]$ in \emph{all} $30{,}000$ trials (pooled observed range $[1.2{\times}10^{-5},\,0.850]$; zero out-of-bounds), confirming the closed-form bound. Sign preservation of $\hat\tau_{\mathrm{dir}}$ therefore reduces to $\operatorname{sign}(\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{total}})$.
\item \textbf{Non-proportional} ($f\sim\mathcal{N}(0,I_K)$ drawn independently of $g$, restricted to $|\tau_{\mathrm{ind}}|\geq 0.05$ to avoid the ill-conditioned regime where $\lambda{=}1{-}B/\tau_{\mathrm{ind}}$ diverges): $\lambda(C)$ exceeds $[0,1]$ in $\sim15$--$17\%$ of trials, illustrating that (a) and (b) are not automatic outside the proportional case.
\end{itemize}
The diagnostic $\lambda_V(C)$ lies in $[0,1]$ across all $30{,}000$ trials irrespective of regime, as guaranteed by the law of total variance. The script and full results are in \texttt{artifacts/verify\_lambda\_bound.py} and \texttt{artifacts/verify\_lambda\_bound\_results.json}.

\subsection{Proof of Theorem~\ref{thm:k3_symmetric} ($K{=}3$ Symmetric Monotonicity)}
\label{app:k3_symmetric_proof}

\input{k3_symmetric_proof.tex}

\begin{remark}[Empirical symmetry of LLM confusion matrices]
\label{rem:symmetry}
Empirical $K{=}3$ confusion matrices from LLM stance detection deviate substantially from symmetry (median relative Frobenius distance $d_{\mathrm{rel}}{=}0.33$; range 0.30--0.38 across 10 VAST configurations). Theorem~\ref{thm:k3_symmetric} thus serves as a theoretical anchor establishing that exact monotonicity is achievable at $K{=}3$ under the strongest structural assumption; practical coverage for asymmetric matrices relies on the numerical evidence of Conjecture~\ref{conj:monotone} ($\varepsilon < 0.02$ across 545{,}945 adversarial curves including non-symmetric structures).
\end{remark}

\subsection{Collider Topology ($A$ = Collider)}
\label{app:collider}

\paragraph{Data-generating process.}
$T\to A\leftarrow Y$, $T\to Y$.
The treatment effect is unconfounded:
\begin{align*}
  T &\sim \mathcal{N}(0,1),\quad
  Y = \tau\,T + \varepsilon_Y,\quad \varepsilon_Y\sim\mathcal{N}(0,1),\;\varepsilon_Y\perp T,\\
  A\mid T,Y &\sim \operatorname{Categorical}\bigl(\operatorname{softmax}(\boldsymbol{\delta}_T\,T + \boldsymbol{\delta}_Y\,Y)\bigr).
\end{align*}
The collider $A$ is a common \emph{effect} of $T$ and $Y$.
The no-control estimator (regressing $Y$ on $T$ alone) is consistent: $\operatorname{plim}\hat\tau_{\mathrm{nc}}=\tau$.
The researcher erroneously controls for $A^*$, inducing Berkson's bias.

\paragraph{Bias decomposition.}
Unlike confounding, $\varepsilon_Y$ is not independent of $A$ (since $A$ depends on $Y=\tau T+\varepsilon_Y$), so Lemma~\ref{lem:bias_decomp} does not apply directly.
Instead, we derive the bias from the FWL identity~\eqref{eq:fwl}.
Writing $\tilde{Y}=\tau\tilde{T}+(\varepsilon_Y-\mathbb{E}[\varepsilon_Y\mid A^*])$ and noting that $\mathbb{E}[\tilde{T}\,\varepsilon_Y]=\mathbb{E}[\operatorname{Cov}(T,\varepsilon_Y\mid A^*)]$ (since $T\perp\varepsilon_Y$ unconditionally):
\begin{equation}
  \operatorname{plim}\hat\tau(C) = \tau + B(C),\quad
  B(C) = \frac{\mathbb{E}[\operatorname{Cov}(T,\,\varepsilon_Y\mid A^*)]}{\mathbb{E}[\operatorname{Var}(T\mid A^*)]}.
  \label{eq:bias_col}
\end{equation}
Under non-differential misclassification ($A^*\perp(T,\varepsilon_Y)\mid A$), define $g(k)=\mathbb{E}[T\mid A{=}k]$ and $h(k)=\mathbb{E}[\varepsilon_Y\mid A{=}k]$.
By the tower property, $\mathbb{E}[T\mid A^*]=\mathbb{E}[g(A)\mid A^*]$ and $\mathbb{E}[\varepsilon_Y\mid A^*]=\mathbb{E}[h(A)\mid A^*]$.
Since $\operatorname{Cov}(T,\varepsilon_Y)=0$, the law of total covariance gives:
\[
  B(C) = \frac{-\operatorname{Cov}\bigl(\mathbb{E}[g(A)\mid A^*],\;\mathbb{E}[h(A)\mid A^*]\bigr)}{\mathbb{E}[\operatorname{Var}(T\mid A^*)]}.
\]

\paragraph{Endpoint analysis.}
At $C=I$ ($A^*{=}A$):
$B(I) = -\operatorname{Cov}(g(A),h(A))\,/\,\mathbb{E}[\operatorname{Var}(T\mid A)] = B_{\mathrm{Berkson}}$,
the full Berkson's bias from conditioning on the collider.

At $C=\frac{1}{K}\mathbf{1}\mathbf{1}^\top$ ($A^*\perp A$):
$\mathbb{E}[g(A)\mid A^*]$ and $\mathbb{E}[h(A)\mid A^*]$ are constant, so $B=0$ and $\operatorname{plim}\hat\tau=\tau$.

This is the structural reverse of confounding: misclassification attenuates the collider-induced bias toward zero, rather than growing bias from zero toward $B_{\mathrm{OV}}$.

\begin{proposition}[Collider sign preservation --- proportional case]
\label{prop:col_prop}
If $h(A)=\lambda\,g(A)$ for some constant $\lambda$, then $|B(C)|\leq|B(I)|$ for all column-stochastic $C$, and $B(C)$ has the same sign as $B(I)$.
Consequently, $\operatorname{sign}(\operatorname{plim}\hat\tau)=\operatorname{sign}(\tau)$ whenever $\operatorname{sign}(\tau)=\operatorname{sign}(\tau+B_{\mathrm{Berkson}})$.
\end{proposition}

\begin{proof}
When $h=\lambda g$, the numerator becomes $-\lambda\,\operatorname{Var}(\mathbb{E}[g(A)\mid A^*])$ and the denominator is $\operatorname{Var}(T)-\operatorname{Var}(\mathbb{E}[g(A)\mid A^*])$.
Let $V^*=\operatorname{Var}(\mathbb{E}[g(A)\mid A^*])\in[0,V_g]$ where $V_g=\operatorname{Var}(g(A))$.
Then
\[
  |B(C)| = |\lambda|\,\frac{V^*}{\operatorname{Var}(T)-V^*}.
\]
The function $x\mapsto x/(a-x)$ is increasing on $[0,a)$.
With $a=\operatorname{Var}(T)>V_g\geq V^*$:
$|B(C)|\leq|\lambda|\,V_g/(\operatorname{Var}(T)-V_g)=|B(I)|$.
At $V^*{=}0$ (uniform $C$): $B{=}0$.  At $V^*{=}V_g$ ($C{=}I$): $B{=}B(I)$.
\end{proof}

\begin{proposition}[Collider sign preservation --- general case]
\label{prop:col_gen}
Define
\begin{equation}
  \bar{B}_{\mathrm{col}} = \frac{\sqrt{\operatorname{Var}(g(A))\,\operatorname{Var}(h(A))}}{\mathbb{E}[\operatorname{Var}(T\mid A)]}.
  \label{eq:bbar_col}
\end{equation}
If $|\tau|>\bar{B}_{\mathrm{col}}$, then $\operatorname{sign}(\operatorname{plim}\hat\tau(C))=\operatorname{sign}(\tau)$ for all column-stochastic $C$.
\end{proposition}

\begin{proof}
\emph{Step~1: Bound the numerator.}
By Cauchy--Schwarz:
$|\operatorname{Cov}(\mathbb{E}[g\mid A^*],\mathbb{E}[h\mid A^*])|\leq\sqrt{\operatorname{Var}(\mathbb{E}[g\mid A^*])\,\operatorname{Var}(\mathbb{E}[h\mid A^*])}$.
Since conditional expectation reduces variance: $\operatorname{Var}(\mathbb{E}[g\mid A^*])\leq\operatorname{Var}(g(A))$ and likewise for $h$.

\emph{Step~2: Bound the denominator.}
$\mathbb{E}[\operatorname{Var}(T\mid A^*)]=\operatorname{Var}(T)-\operatorname{Var}(\mathbb{E}[T\mid A^*])\geq\operatorname{Var}(T)-\operatorname{Var}(g(A))=\mathbb{E}[\operatorname{Var}(T\mid A)]$.

\emph{Step~3: Conclude.}
$|B(C)|\leq\sqrt{\operatorname{Var}(g)\,\operatorname{Var}(h)}\,/\,\mathbb{E}[\operatorname{Var}(T\mid A)]=\bar{B}_{\mathrm{col}}$.
When $|\tau|>\bar{B}_{\mathrm{col}}$, the bias cannot overwhelm $\tau$. In practice, $\operatorname{Var}(g(A))$ and $\operatorname{Var}(h(A))$ can be estimated from observable data by bootstrapping regression coefficients from $A^*$-stratified subsamples, yielding conservative upper bounds on $\bar{B}_{\mathrm{col}}$.
\end{proof}

\paragraph{Verification for paper parameters ($K{=}3$).}
With $\tau{=}1.0$, $\boldsymbol{\delta}_T{=}(0,\,0.5,\,{-}0.3)$, $\boldsymbol{\delta}_Y{=}(0,\,0.8,\,0.6)$, from $5{\times}10^6$-sample Monte Carlo:
\begin{align*}
  \operatorname{Var}(g(A)) &= 0.187,\quad
  \operatorname{Var}(h(A)) = 0.076,\quad
  \mathbb{E}[\operatorname{Var}(T\mid A)] = 0.813,\\
  \bar{B}_{\mathrm{col}} &= \sqrt{0.187\times 0.076}\;/\;0.813 = 0.147.
\end{align*}
Since $|\tau|{=}1.0\gg\bar{B}_{\mathrm{col}}{=}0.147$, Proposition~\ref{prop:col_gen} guarantees sign preservation.
Additionally, $B_{\mathrm{Berkson}}{=}{-}0.114$, giving $\tau+B_{\mathrm{Berkson}}{=}0.886{>}0$.
Verified across $100{,}000$ random column-stochastic $C$: $\tau_{\mathrm{sign\,flip}}{=}0\%$, and $|B(C)|\leq|B_{\mathrm{Berkson}}|$ in all cases.

\subsection{Front-door Topology ($A$ = Mediator under Confounding)}
\label{app:frontdoor}

\paragraph{Data-generating process.}
$T\to A\to Y$, $U\to T$, $U\to Y$.
An unobserved confounder $U$ affects both treatment and outcome:
\begin{align*}
  U &\sim \mathcal{N}(0,1),\quad
  T = \alpha_U\,U + \eta,\quad \eta\sim\mathcal{N}(0,1),\;\eta\perp U,\\
  A\mid T &\sim \operatorname{Categorical}\bigl(\operatorname{softmax}(\boldsymbol{\gamma}+\boldsymbol{\delta}\,T)\bigr),\\
  Y &= f(A) + \lambda\,U + \varepsilon_Y,\quad \varepsilon_Y\perp(T,A,U).
\end{align*}
There is no direct $T{\to}Y$ edge; the causal effect of $T$ on $Y$ operates entirely through $A$.
The researcher controls for $A^*$ in $Y\sim 1+T+D_1^*+\cdots+D_{K-1}^*$.

\paragraph{Bias decomposition.}
Since the direct effect is zero, the FWL coefficient on $T$ captures only bias:
\[
  \operatorname{plim}\hat\tau(C) = \frac{\mathbb{E}[\operatorname{Cov}(T,\,Y\mid A^*)]}{\mathbb{E}[\operatorname{Var}(T\mid A^*)]}.
\]
Substituting $Y=f(A)+\lambda U+\varepsilon_Y$ and using $\varepsilon_Y\perp(T,A,A^*)$:
\begin{equation}
  \operatorname{plim}\hat\tau(C)
  = \underbrace{\frac{\lambda\,\alpha_U}{\operatorname{Var}(T)}}_{\text{confounding (constant)}}
  \;+\; \underbrace{\frac{\mathbb{E}[\operatorname{Cov}(T,\,f(A)\mid A^*)]}{D(C)}}_{\displaystyle B_{\mathrm{med}}(C)},
  \label{eq:fd_decomp}
\end{equation}
where $D(C)=\mathbb{E}[\operatorname{Var}(T\mid A^*)]$.

\begin{proof}[Derivation of~\eqref{eq:fd_decomp}]
Expand the numerator as
$\mathbb{E}[\operatorname{Cov}(T,f(A)\mid A^*)] + \lambda\,\mathbb{E}[\operatorname{Cov}(T,U\mid A^*)]$.
For the confounding term: since $T=\alpha_U U+\eta$ with $(U,\eta)$ jointly Gaussian, the regression residual $\xi=U-\frac{\alpha_U}{\operatorname{Var}(T)}T$ satisfies $\xi\perp T$.
Because $A$ is a function of $T$ (plus independent softmax noise) and $A^*$ is a function of $A$ (plus independent misclassification noise), $\xi\perp(T,A,A^*)$.
Therefore
\[
  \operatorname{Cov}(T,U\mid A^*{=}j)
  = \frac{\alpha_U}{\operatorname{Var}(T)}\,\operatorname{Var}(T\mid A^*{=}j)
  \quad\forall\;j,
\]
and $\lambda\,\mathbb{E}[\operatorname{Cov}(T,U\mid A^*)]=\frac{\lambda\alpha_U}{\operatorname{Var}(T)}\,D(C)$, yielding the constant first term after division by $D(C)$.
\end{proof}

This decomposition reveals that \emph{misclassification does not affect the confounding component}.
The confusion matrix $C$ modulates only the mediation-type leakage $B_{\mathrm{med}}(C)$, which has the same structure as in~§\ref{app:mediation}.

\paragraph{Endpoint analysis.}
At $C=I$: $B_{\mathrm{med}}(I)=0$ (controlling $A$ blocks the $T\to A\to Y$ path), and $\operatorname{plim}\hat\tau(I)=\lambda\alpha_U/\operatorname{Var}(T)$ (pure confounding residual).

At $C=\frac{1}{K}\mathbf{1}\mathbf{1}^\top$: $B_{\mathrm{med}}=\operatorname{Cov}(T,f(A))/\operatorname{Var}(T)$, and $\operatorname{plim}\hat\tau_{\mathrm{nc}}=[\operatorname{Cov}(T,f(A))+\lambda\alpha_U]/\operatorname{Var}(T)$, the naive regression coefficient with no controls.

\paragraph{Numerical Verification (Front-door sign preservation).}
\label{find:fd_sign}
Since $B_{\mathrm{med}}(C)$ has the same structure as the mediation bias (§\ref{app:mediation})---interpolating between $0$ at $C{=}I$ and $\operatorname{Cov}(T,f(A))/\operatorname{Var}(T)$ at $C$ uniform---sign preservation holds whenever the two endpoints have the same sign:
\[
  \operatorname{sign}\!\bigl(\lambda\alpha_U\bigr)
  = \operatorname{sign}\!\bigl(\lambda\alpha_U + \operatorname{Cov}(T,f(A))\bigr).
\]
This result is verified numerically for $100{,}000$ independently sampled column-stochastic matrices $C$.

\paragraph{Verification for paper parameters ($K{=}3$).}
With $\alpha_U{=}1.0$, $\lambda{=}1.0$, $\boldsymbol{\beta}{=}(0,1.0,{-}0.5)$, $\boldsymbol{\gamma}{=}(0,0.5,{-}0.3)$, $\boldsymbol{\delta}{=}(0,1.0,{-}0.8)$:
\begin{align*}
  \lambda\alpha_U/\operatorname{Var}(T) &= 1.0\times 1.0\,/\,2.0 = 0.500,\\
  \operatorname{Cov}(T,f(A))/\operatorname{Var}(T) &= 0.594\,/\,2.0 = 0.297.
\end{align*}
Both components are positive, so $\operatorname{plim}\hat\tau(C)\in[0.500,\,0.797]>0$ for all $C$.

\subsection{Exposure Topology: Sign Reversal Counterexample}
\label{app:exposure}

\begin{proposition}[Exposure sign reversal]
\label{prop:exp_counter}
For $K{=}3$ with $A$ as the treatment variable, there exist a marginal distribution $\mathbf{p}$, treatment effects $\boldsymbol{\beta}$, and a column-stochastic $C$ with $C_{kk}>0.5$ for all $k$, such that $\operatorname{sign}(\operatorname{plim}\hat\beta_1)\neq\operatorname{sign}(\beta_1)$.
\end{proposition}

\begin{proof}[Construction]
Consider the exposure DGP $Y = \beta_1 D_1 + \beta_2 D_2 + \varepsilon_Y$ with:
\begin{itemize}
  \item Uniform prior: $\mathbf{p}=(1/3,\,1/3,\,1/3)$.
  \item True effects: $\beta_1=1$,\; $\beta_2=-10$.
\end{itemize}
Choose the confusion matrix
\[
  C = \begin{pmatrix}
    0.60 & 0.10 & 0.10\\
    0.10 & 0.60 & 0.39\\
    0.30 & 0.30 & 0.51
  \end{pmatrix},\qquad
  C_{kk}\in\{0.60,\,0.60,\,0.51\},\;\text{all}>0.5.
\]
Column sums are $1$ (verifiable by inspection).

The OLS estimator regresses $Y$ on $(1,D_1^*,D_2^*)$.
The plim of $\hat\beta_j$ equals $\mathbb{E}[Y\mid A^*{=}j]-\mathbb{E}[Y\mid A^*{=}0]$, where
\[
  \mathbb{E}[Y\mid A^*{=}j] = \sum_{k=0}^{2} P(A{=}k\mid A^*{=}j)\,\beta_k
  = \sum_{k=1}^{2}\frac{C_{jk}\,p_k}{q_j}\,\beta_k.
\]

\emph{Marginal distribution of $A^*$:}
\begin{align*}
  q_0 &= \tfrac{1}{3}(0.60+0.10+0.10)=0.267,\\
  q_1 &= \tfrac{1}{3}(0.10+0.60+0.39)=0.363,\\
  q_2 &= \tfrac{1}{3}(0.30+0.30+0.51)=0.370.
\end{align*}

\emph{Conditional means:}
\begin{align*}
  \mathbb{E}[Y\mid A^*{=}0]
  &= \frac{0.10\times\tfrac{1}{3}\times 1 + 0.10\times\tfrac{1}{3}\times({-}10)}{0.267}
  = \frac{0.0333-0.3333}{0.267} = -1.125,\\[4pt]
  \mathbb{E}[Y\mid A^*{=}1]
  &= \frac{0.60\times\tfrac{1}{3}\times 1 + 0.39\times\tfrac{1}{3}\times({-}10)}{0.363}
  = \frac{0.200-1.300}{0.363} = -3.028,\\[4pt]
  \mathbb{E}[Y\mid A^*{=}2]
  &= \frac{0.30\times\tfrac{1}{3}\times 1 + 0.51\times\tfrac{1}{3}\times({-}10)}{0.370}
  = \frac{0.100-1.700}{0.370} = -4.324.
\end{align*}

\emph{Estimated coefficient:}
\[
  \operatorname{plim}\hat\beta_1 = \mathbb{E}[Y\mid A^*{=}1]-\mathbb{E}[Y\mid A^*{=}0]
  = -3.028-(-1.125) = \mathbf{-1.903}.
\]
The true $\beta_1=+1>0$ but $\operatorname{plim}\hat\beta_1=-1.903<0$: \textbf{sign reversal}.

\emph{Mechanism.}
The large off-diagonal entry $C_{12}=0.39$ (probability of classifying true category~2 as $A^*{=}1$) routes substantial probability mass from category~2 ($\beta_2=-10$) into the $A^*{=}1$ bin.
Despite $C_{11}=0.60>0.5$, the contamination from the strongly negative $\beta_2$ overwhelms the positive $\beta_1$, reversing the sign of the estimated effect for category~1.
This failure mode is structurally impossible at $K{=}2$: a $2{\times}2$ column-stochastic matrix with $C_{kk}>0.5$ has the form $C=\bigl(\begin{smallmatrix}a&1{-}b\\1{-}a&b\end{smallmatrix}\bigr)$ with $a,b>0.5$, and the single coefficient $\hat\beta_1$ is a positive-weighted average of $\beta_1$, preserving sign.
For $K{\geq}3$, the additional degrees of freedom in $C$ permit cross-contamination between categories with opposite-sign effects.
\end{proof}

\subsection{Summary of Conditions}
\label{app:conditions}

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Topology} & \textbf{Sufficient condition} & \textbf{Status} & \textbf{Paper DGP} \\
\midrule
Confounding & $\operatorname{sign}(\tau)=\operatorname{sign}(\tau{+}B_{\mathrm{OV}})$ & Proved & $\checkmark$ ($2.0$ vs $2.3$)\\
Collider & $|\tau|>\bar{B}_{\mathrm{col}}$ (Prop.~\ref{prop:col_gen}) & Proved & $\checkmark$ ($1.0$ vs $0.15$)\\
\midrule
Mediation & $\operatorname{sign}(\tau_{\mathrm{dir}})=\operatorname{sign}(\tau_{\mathrm{total}})$ & Empirical & $\checkmark$ ($0.5$ vs $0.85$)\\
M-bias & Bias is attenuating (toward $\tau$) & Empirical & $\checkmark$ \\
Front-door & $\operatorname{sign}(\lambda\alpha_U)=\operatorname{sign}(\lambda\alpha_U{+}\operatorname{Cov}(T,f))$ & Empirical & $\checkmark$ ($0.50$ vs $0.80$)\\
\midrule
Exposure & Not guaranteed for $K\geq 3$ & Counterex. & See §\ref{app:exposure} \\
IV & Not guaranteed (denominator instability) & --- & --- \\
\bottomrule
\end{tabular}
\caption{Sufficient conditions for sign preservation under non-differential misclassification.
``Proved'' = algebraic proof via Cauchy--Schwarz (confounding) or Berkson attenuation (collider).
``Empirical'' = verified across 100{,}000 random column-stochastic matrices with zero violations; analytic proof for general $K$ remains open.
All conditions are on DGP parameters (not on $C$): once satisfied, sign preservation holds for all column-stochastic $C$ with $C_{kk}>0$.}
\label{tab:sign_conditions}
\end{table}

\paragraph{Structural argument for M-bias topology.}
\emph{M-bias:} The M-structure creates a backdoor path through two latent common causes.
Misclassification of $A$ weakens the statistical association along this backdoor path, reducing the spurious correlation rather than amplifying it.
The bias therefore interpolates between the full M-bias (perfect classification, $\delta{=}0$) and zero (no classification information, $\delta{\to}\delta_{\max}$), monotonically non-increasing in $\delta$.

\paragraph{M-bias sign-flip ASV $= \infty$.}
For the four covariate topologies (confounding, mediation, collider, M-bias), FWL invariance (Appendix~\ref{app:fwl}) implies that the coefficient on $T$ depends on $A^*$ only through the residualized relationship $\tilde{T} = T - \mathbb{E}[T \mid A^*]$. For M-bias, the spurious path through $A$ is the sole source of bias, and misclassification attenuates this path for all column-stochastic $C$. Since $\text{bias}(C(\delta))$ is monotonically non-increasing in $\delta$ and starts at a value with the same sign as $\tau$, no $\delta \in [0, \delta_{\max}]$ can produce a sign reversal. Therefore $\delta^*_{\text{sign}} = \infty$, which is a direct consequence of FWL invariance combined with the protective monotonicity of M-bias misclassification.

\paragraph{Remark.}
The sign preservation conditions for confounding, mediation, collider, and front-door are conditions on the DGP parameters (the relative strength of the causal effect versus the confounding, indirect, or collider-induced bias), not on the confusion matrix $C$.
Once the condition is satisfied, sign preservation holds for \emph{all} column-stochastic $C$ with $C_{kk}>0$---no diagonal-dominance restriction is needed.
Conversely, if the condition fails (i.e., the omitted-variable or total-effect estimate has the opposite sign from $\tau$), then there exist confusion matrices that produce sign reversal.
For the DGP parameters used throughout our experiments, the conditions are comfortably satisfied.
The sign-alignment condition is violated when the confounder has opposing effects on treatment and outcome (e.g., $\gamma_A > 0$ on $T$ but $\gamma_A < 0$ on $Y$), producing $\operatorname{sign}(\tau) \neq \operatorname{sign}(\tau_{\mathrm{OV}})$. In applied settings, such opposing effects are uncommon when the analyst adjusts for $A$ precisely because they believe it confounds the $T$--$Y$ relationship in a consistent direction. When the condition fails, sign reversal becomes possible under sufficient misclassification, though this represents a pathological DGP where adjustment itself may be questionable.

\subsection{Adversarial Monotonicity Verification}
\label{app:adversarial_monotonicity}

We test Conjecture~\ref{conj:monotone} under four adversarial confusion matrix structures designed to challenge envelope monotonicity: near-permutation ($C_0 = (1{-}\epsilon)P + \epsilon \cdot \mathbf{1}\mathbf{1}^\top/K$, $P$ a random permutation), block-diagonal (within-block confusion only), sparse (two nonzero entries per column), and near-uniform ($C_{kk} \to 1/K$). For each structure, we sweep $\delta \in [0,1]$ along $C(\delta) = (1{-}\delta)I + \delta C_0$ and measure the maximum single-step monotonicity violation $\varepsilon_{\mathrm{abs}}$.

\begin{table}[h]
\centering
\small
\caption{Adversarial monotonicity test across $K{=}3,5,7$, seven topologies, and three $\beta$ configurations ($N{=}1{,}000$ matrices per cell, 545{,}945 total curves).}
\label{tab:adversarial_monotonicity}
\begin{tabular}{lcc}
\toprule
\textbf{Structure} & \textbf{Non-monotone \%} & \textbf{Max $\varepsilon_{\mathrm{abs}}$} \\
\midrule
Near-permutation & 35.3\% & 0.016 \\
Block-diagonal & 0\% & 0 \\
Sparse & 2.3\% & 0.003 \\
Near-uniform & 0\% & 0 \\
\midrule
\textbf{All structures} & 11.9\% & \textbf{0.016} \\
\bottomrule
\end{tabular}
\end{table}

The four families span the extremes of the column-stochastic polytope: diagonal-dominant (near-identity), maximally reordered (near-permutation), degenerate (rank-deficient sparse), and block-structured heterogeneity (block-diagonal). The global maximum violation $\varepsilon_{\mathrm{abs}} = 0.016 < 0.02$ confirms that Conjecture~\ref{conj:monotone} holds under adversarial matrix structures. Near-permutation matrices with alternating-sign $\beta$ coefficients represent the most challenging case; even here, violations remain below the conjectured bound. Block-diagonal and near-uniform structures show exact monotonicity.

\section{Supplementary Tables, Figures, and Algorithm}
\label{app:supplementary}

\subsection{Binarization Collapse}
\label{app:binarization}

Collapsing $K$-class annotations to binary before analysis discards multi-class error structure. We quantify ``binarization collapse'' as $\Delta = |\text{bias}_{K} - \text{bias}_{K \to 2}|$.

For $K{=}3$, binarization introduces additional bias varying $>$150$\times$ across topologies: from $\bar{\Delta}{=}0.006$ (M-bias) to $\bar{\Delta}{=}0.926$ (IV). Exposure and IV also introduce sign flips during binarization (14\% and 17.5\%), a phenomenon absent in the other topologies (Figure~\ref{fig:binarization}).


\begin{table}[h]
\centering
\caption{ASV type applicability and sensitivity across DAG topologies. Magnitude ASV at $\tau{=}0.5$, 10\% threshold. Power at $\tau{=}0.05$ significance filter. ---: not applicable.}
\label{tab:asv_grid}
\small
\begin{tabular}{lcccl}
\toprule
\textbf{Topology} & \textbf{Mag. ASV} & \textbf{Signif.} & \textbf{Sign-flip} & \textbf{Rank} \\
\midrule
IV & 0.101--0.607 & 0\% & 1.1--6.7 & 1 (fragile) \\
Front-door & 0.064--0.387 & 100\% & --- & 2 \\
Confounding & 0.081--0.487 & 100\% & --- & 3 \\
Mediation & 0.076--0.454 & 0\% & --- & 4 \\
Collider & 0.338--2.028 & 0\% & --- & 5 \\
M-bias & $\infty$ & 96--100\% & --- & 6 (immune) \\
Exposure & --- & --- & 83.4\%$^*$ & --- \\
\bottomrule
\multicolumn{5}{l}{\scriptsize $^*$Fraction of $C_0$ with $\tau_{\text{sign\_flip}} < 1$. Exposure uses sign-flip ASV exclusively.}
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig7_dirichlet_overlay.pdf}
\caption{Empirical LLM confusion matrices overlaid on theoretical violation-rate curves (exposure, left; IV, right). Theoretical curves are derived from $K{=}3$ Dirichlet random matrices; empirical points are positioned at their minimum diagonal entry. Markers denote datasets (circles: HumAID $K{=}10$; squares: VAST $K{=}3$; triangles: CivilComments $K{=}2$).}
\label{fig:dirichlet_overlay}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig4_binarization.pdf}
\caption{Binarization collapse $\Delta$ across topologies. Collapsing $K{=}3$ annotations to binary introduces additional bias varying $>$150$\times$ from M-bias (0.006) to IV (0.926).}
\label{fig:binarization}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig6_asv_heatmap.pdf}
\caption{ASV magnitude ranges across topologies for HumAID ($K{=}10$). Left: magnitude ASV ($\delta^*$) range across 8 LLM configurations; lower values indicate greater fragility. ``Safe'' topologies never exceed the 10\% bias threshold. Right: percentage of configurations flagged as ASV-unsafe.}
\label{fig:asv_heatmap}
\end{figure}

\begin{table}[h]
\centering
\caption{RMSE reduction (\%) by correction method and DAG topology (mean across 6 error structures, $N_{\text{sim}}{=}1{,}000$). Positive = reduced RMSE; negative = increased RMSE. Naive/MLA show 0.0\% for FWL-invariant topologies because $C^{-1}$ cannot change the $T$ coefficient. \textbf{Bold}: best method per topology; \underline{underline}: best gold-free method.}
\label{tab:correction}
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Topology} & \textbf{Naive} & \textbf{MLA} & \textbf{DSL} & \textbf{SIMEX} & \textbf{PPI++} \\
\midrule
Confounding & 0.0 & 0.0 & \underline{48.0} & 35.1 & \textbf{83.2} \\
Mediation & 0.0 & 0.0 & \underline{52.9} & 39.1 & \textbf{54.1} \\
Collider & 0.0 & 0.0 & $-$92.6 & \underline{31.8} & \textbf{50.5} \\
M-bias & 0.0 & 0.0 & $-$267 & \textbf{\underline{0.1}} & $-$88.0 \\
\midrule
Front-door & $-$52.7 & $-$111.5 & \underline{$-$29.0} & $-$62.4 & \textbf{74.1} \\
Exposure & $-$44.8 & $-$89.0 & $-$59.5 & \underline{$-$9.8} & \textbf{75.5} \\
IV & $-$40.4 & $-$58.7 & $-$206.1 & \underline{59.4} & \textbf{79.4} \\
\midrule
Grand mean & $-$19.7 & $-$37.0 & $-$79.0 & 13.3 & \textbf{47.0} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Error structures used in correction experiments.}
The six confusion matrix structures used for Table~\ref{tab:correction} are:
(1)~\emph{Diagonal-dominant}: $C_{kk}{=}0.7$, off-diagonal uniform;
(2)~\emph{Moderate noise}: $C_{kk}{=}0.5$, off-diagonal uniform;
(3)~\emph{Near-permutation}: $C_{kk}{=}0.4$, largest off-diagonal 0.5;
(4)~\emph{Asymmetric}: $C_{kk}$ varies (0.8, 0.5, 0.3);
(5)~\emph{Block confusion}: two classes confused ($C_{12}{=}C_{21}{=}0.4$), third clean;
(6)~\emph{Heavy noise}: $C_{kk}{=}0.4$, off-diagonal uniform.
All are column-stochastic with $K{=}3$. Results are averaged across all six structures.

\begin{algorithm}[h]
\caption{Correction method selection for LLM-annotated causal inference. This topology-based classification is robust to $\varepsilon$-approximate deviations; practitioners computing quantitative ASV thresholds should note the $\varepsilon$-approximate bounds (Conjecture~\ref{conj:monotone}, Section~\ref{sec:asv}). Assumes linear structural equations; FWL-based routing may not apply under nonlinear models (see Appendix~\ref{sec:logistic_dgp}). Thresholds calibrated for $K{=}3$ linear SEM; recalibrate for different $K$ using topology-specific bias functions.}
\label{alg:correction}
\small
\begin{algorithmic}[1]
\REQUIRE DAG topology $\mathcal{T}$, gold-label availability $G \in \{\textsc{yes}, \textsc{no}\}$
\ENSURE Recommended method $M$, risk level $R$
\STATE
\STATE \textbf{Step 0: DAG uncertainty} \COMMENT{If $\mathcal{T}$ is uncertain, repeat Steps 1--3 under each candidate topology and report the range}
\STATE
\STATE \textbf{Step 1: Assess risk level}
\IF{$\mathcal{T} \in \{\text{M-bias}\}$}
    \STATE $R \gets \textsc{Low}$ \COMMENT{$|\text{bias}| < 0.04$ for all $C$}
\ELSIF{$\mathcal{T} \in \{\text{Conf., Med., Coll., Front-door}\}$}
    \STATE $R \gets \textsc{Medium}$ \COMMENT{bounded bias, no sign reversal}
\ELSE
    \STATE $R \gets \textsc{High}$ \COMMENT{Exposure, IV: sign reversal 83.4\% / 52.6\%}
\ENDIF
\STATE
\STATE \textbf{Step 2: Select correction method}
\IF{$G = \textsc{yes}$}
    \IF{$\mathcal{T} = \text{M-bias}$}
        \STATE $M \gets \textsc{None}$ \COMMENT{PPI++ RMSE red.\ $-$88.0\%; no correction preferred}
    \ELSE
        \STATE $M \gets \text{PPI++}$ \COMMENT{47.0\% RMSE red., 96.8\% coverage}
    \ENDIF
\ELSE
    \STATE \COMMENT{no gold labels}
    \IF{$\mathcal{T} \in \{\text{Confounding, Mediation}\}$}
        \STATE $M \gets \text{DSL}$ \COMMENT{48.0\% / 52.9\% RMSE reduction}
    \ELSIF{$\mathcal{T} = \text{IV}$}
        \STATE $M \gets \text{MC-SIMEX}$ \COMMENT{59.4\% RMSE reduction}
    \ELSIF{$\mathcal{T} \in \{\text{Collider, M-bias}\}$}
        \STATE $M \gets \text{MC-SIMEX}$ \COMMENT{31.8\% / 0.1\% RMSE reduction}
    \ELSE
        \STATE $M \gets \emptyset$; flag: acquire gold labels \COMMENT{no reliable gold-free method}
    \ENDIF
\ENDIF
\STATE
\STATE \textbf{Step 3: FWL invariance warning}
\IF{$\mathcal{T} \in \{\text{Conf., Med., Coll., M-bias}\}$}
    \STATE Skip Naive plug-in and MLA \COMMENT{0\% bias red.\ on $T$ coeff.\ (Table~\ref{tab:correction}: below 2\%, no method improves)}
\ENDIF
\RETURN $(M, R)$
\end{algorithmic}
\end{algorithm}

\subsection{Proof of FWL Invariance}
\label{app:fwl_proof}

For the four topologies where $A$ enters as a covariate (confounding, mediation, collider, M-bias), the regression is $Y = \tau T + \boldsymbol{\gamma}^\top \mathbf{D}^* + \varepsilon$, where $\mathbf{D}^*$ is the $N \times (K{-}1)$ dummy matrix for $A^*$. Any matrix-based correction replaces $\mathbf{D}^*$ with $\tilde{\mathbf{D}} = \mathbf{D}^* M$ for some invertible $(K{-}1) \times (K{-}1)$ matrix $M$ (e.g., $M = C^{-1}$ for plug-in, or $M = (C^\top C)^{-1} C^\top$ for MLE). By the Frisch--Waugh--Lovell theorem~\cite{lovell1963}, $\hat{\tau}$ depends only on the residual of $T$ projected off $\operatorname{col}(\mathbf{D}^*)$. Since $\operatorname{col}(\mathbf{D}^* M) = \operatorname{col}(\mathbf{D}^*)$ for invertible $M$, the projection is unchanged and $\hat{\tau}(\tilde{\mathbf{D}}) = \hat{\tau}(\mathbf{D}^*)$ exactly. SIMEX adds noise $\mathbf{D}^*_{\text{sim}} = \mathbf{D}^* + \lambda \mathbf{E}$; when $\mathbf{E}$ is generated from the same column space, the extrapolation to $\lambda = -1$ converges to a column-space-equivalent transformation, yielding 0\% bias reduction empirically.

\paragraph{FWL breakdown under logistic regression.}
\label{sec:fwl_logistic}
The column-space argument applies only to estimators whose treatment coefficient is a function of $T$ residualized off $\operatorname{col}(\mathbf{D}^*)$---a property of linear projection. The logistic MLE solves $\sum_i (Y_i - \sigma(\mathbf{x}_i^\top\boldsymbol{\beta}))\mathbf{x}_i = \mathbf{0}$; the implicit weighting by $\sigma'(\cdot)$ depends on the actual values of the design columns, not only on their span, so replacing $\mathbf{D}^*$ by $\mathbf{D}^* M$ generally changes the fitted $\hat\tau$. We verify this on the confounding topology ($K{=}3$, $\tau{=}0.5$, $N{=}10{,}000\times 500$ MC reps) under both linear ($Y = \tau T + \mathbf{D}\boldsymbol{\beta}+\varepsilon$) and logistic ($Y\sim\mathrm{Bern}(\sigma(\tau T + \mathbf{D}\boldsymbol{\beta}))$) DGPs, comparing the uncorrected estimator (fit on $\mathbf{D}^*$) against Naive plug-in (fit on per-sample $(C^{-1}\mathbf{e}_{A^*})_{1:K}$). Per-rep estimates are paired (same $A^*$, same outcome).

\begin{table}[h]
\centering
\small
\caption{FWL invariance under OLS vs. breakdown under logistic regression. $|\Delta|_{\max}=\max_m|\hat\tau_{\mathrm{corr}}^{(m)}-\hat\tau_{\mathrm{un}}^{(m)}|$ over 500 MC reps; bias reduction = $1-|\hat\tau_{\mathrm{corr}}-\tau_*|/|\hat\tau_{\mathrm{un}}-\tau_*|$. Confounding DAG, $K{=}3$, $N{=}10{,}000$.}
\label{tab:fwl_logistic}
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{\textbf{OLS}} & \multicolumn{2}{c}{\textbf{Logistic}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
CM (VAST K=3) & $|\Delta|_{\max}$ & bias red. & $|\Delta|_{\max}$ & bias red. \\
\midrule
deepseek\_zero-shot     & $2.3\!\times\!10^{-15}$ & $+0.00\%$ & $8.1\!\times\!10^{-4}$ & $-0.09\%$ \\
llama4\_few-shot-3      & $5.2\!\times\!10^{-15}$ & $+0.00\%$ & $6.8\!\times\!10^{-4}$ & $-0.07\%$ \\
llama4\_few-shot-5      & $4.3\!\times\!10^{-15}$ & $+0.00\%$ & $6.8\!\times\!10^{-4}$ & $-0.06\%$ \\
llama4\_zero-shot       & $1.6\!\times\!10^{-15}$ & $+0.00\%$ & $5.9\!\times\!10^{-4}$ & $-0.05\%$ \\
qwen2.5-7b\_few-shot-3  & $7.2\!\times\!10^{-15}$ & $-0.00\%$ & $6.8\!\times\!10^{-4}$ & $-0.01\%$ \\
qwen2.5-7b\_few-shot-5  & $8.2\!\times\!10^{-15}$ & $+0.00\%$ & $5.6\!\times\!10^{-4}$ & $+0.05\%$ \\
\midrule
\textbf{Mean}           & ---                     & $\mathbf{0.0\%}$ & ---            & $\mathbf{-0.04\%}$ \\
\bottomrule
\end{tabular}
\end{table}

Two findings: (i) Under OLS, $\hat\tau_{\mathrm{corr}}^{(m)}=\hat\tau_{\mathrm{un}}^{(m)}$ to machine precision in every replication, confirming Proposition~\ref{app:fwl_proof} as an exact algebraic identity. (ii) Under logistic regression, per-replication estimates differ by $|\Delta|_{\max}\!\sim\!10^{-3}$---thirteen orders of magnitude larger than the OLS gap---so the invariance fails formally. However, the directional effect on bias is negligible: mean bias reduction is $-0.04\%$ across the six confusion matrices (range $[-0.09\%, +0.05\%]$, not even consistent in sign). Matrix-based corrections remain practically useless under logistic regression---not by an exact invariance, but because the deviation is too small to systematically offset the misclassification-induced bias (which itself is $\sim\!0.10$ in absolute units, two orders of magnitude larger than $|\Delta|$). The routing rule of Algorithm~\ref{alg:correction} (skip Naive/MLA for these four topologies) is therefore conservative-but-correct under logistic regression as well. Script: \texttt{artifacts/exp\_fwl\_logistic\_breakdown.py}; results: \texttt{exp\_fwl\_logistic\_results.json}.

\subsection{Applied Illustration: ClinicalBERT Case Study}
\label{app:case_study}

To demonstrate the framework's end-to-end utility, we apply it retrospectively to \citet{lee_wood_doughty2024}, who use ClinicalBERT to classify patient smoking status ($K{=}4$: never, past, current, unknown) from clinical notes and control for this predicted confounder when estimating the causal effect of echocardiography on mortality in 4{,}735 MIMIC-III sepsis patients.
Our taxonomy identifies this as a \emph{confounding topology}: the annotated variable enters as a confounder between treatment and outcome.
Three conclusions follow directly.
First, the zero sign-flip guarantee for confounding (Table~\ref{tab:taxonomy}) ensures that the estimated protective effect (OR~$= 0.89$) cannot reverse direction due to annotation error---a reassurance unavailable without topology-specific analysis.
Second, the FWL invariance result implies that any correction reducing to $C^{-1}$ applied to the dummy-coded confounder achieves exactly 0\% bias reduction for the treatment coefficient; naive plug-in is provably futile here.
Third, our correction guidance ranks PPI++ (83.2\% RMSE reduction for confounding) above MC-SIMEX (35.1\%) when gold labels are available---a concrete recommendation the original study lacked.
This example illustrates the intended workflow: DAG identification $\to$ topology-specific guarantees $\to$ correction method selection.

\subsection{IV Wald Estimator: Diagonal-Dominance Subspaces}
\label{sec:iv_dirichlet_subspace}

Section~\ref{sec:taxonomy_results} reports a 52.6\% Wald sign-flip rate
over the full Dirichlet$(1,1,1)$ prior. Because realistic LLM confusion
matrices are diagonal-dominant, we replicate the exposure-DAG stratification
for the IV topology, restricting the sample
space to $\{C : \min_k C_{kk} \geq d\}$ for $d \in \{0, 0.5, 0.7, 0.9\}$
(100{,}000 random matrices per threshold).

\begin{table}[h]
\centering
\caption{IV Wald estimator bias by minimum-diagonal subspace ($K{=}3$, $\tau_{\text{true}}{=}1.41$).
Rates over 100{,}000 Dirichlet-truncated random $C$ per threshold.}
\label{tab:iv_dirichlet_subspace}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Subspace} & \textbf{Violation} & \textbf{Sign Flip} & \textbf{Amplif.} & \textbf{Worst} \\
\midrule
Full           & 100.0\% & 51.7\% & 48.3\% & $10^{5}\!\times$ \\
$d \geq 0.5$   & 100.0\% & 0.0\%  & 100.0\% & $28.6\times$ \\
$d \geq 0.7$   & 100.0\% & 0.0\%  & 100.0\% & $2.4\times$ \\
$d \geq 0.9$   & 100.0\% & 0.0\%  & 100.0\% & $1.2\times$ \\
\bottomrule
\end{tabular}
\end{table}

The contrast with exposure is instructive: at $d{\geq}0.7$, exposure violation drops to 13.7\% (sign reversal 2.1\%, amplification 11.7\%), while IV maintains 100\% violation with pure amplification. IV sign reversal is entirely driven by pathological (near-random) confusion matrices where the Wald denominator crosses zero; diagonal dominance reduces this crossing to near-zero under empirical accuracy conditions but cannot prevent amplification. Even at $d{\geq}0.9$, the Wald estimator inflates $|\tau|$ by up to $1.24\times$ with probability~1.

\subsection{Non-Differential Misclassification Verification}
\label{sec:nondiff_verify}

The non-differential assumption $P(A^*{=}j \mid A{=}k, Y, \mathbf{Z}) = P(A^*{=}j \mid A{=}k)$ holds \emph{by design} in standard LLM annotation pipelines: the model receives only source text as input and classifies without observing the outcome $Y$ or covariates $\mathbf{Z}$. The annotation API call is structurally independent of downstream variables, satisfying the conditional independence requirement.

To provide empirical support beyond the design argument, we examine topic-stratified confusion matrices using the VAST dataset ($K{=}3$, stance detection), where the \texttt{topic} field (e.g., ``gun control,'' ``college'') serves as a proxy for latent factors correlated with $Y$. Across 10 model configurations (3 LLMs $\times$ 2--4 prompt types) and 19 qualifying topics (minimum 10 samples per topic), we compute per-topic confusion matrices $C_{\text{topic}}(t) = P(A^*{=}j \mid A{=}k, \text{topic}{=}t)$ and compare against the overall $C = P(A^*{=}j \mid A{=}k)$.

Per-topic sample sizes range from 10 to 42, making individual topic CMs inherently noisy. A bootstrap permutation test ($B{=}10{,}000$) under the non-differential null shows that the expected grand maximum deviation $\max_{t,j,k} |C_{\text{topic}}(j,k) - C(j,k)|$ at the 95th percentile ranges from 0.90 to 0.98 across model configurations---driven by the smallest topics. For the 5 largest topics ($n \geq 28$), expected per-topic max deviation at the 95th percentile is 0.38--0.49, consistent with finite-sample variation rather than systematic topic-dependence. The observed per-topic variation is fully consistent with the non-differential null.

This proxy-based test is necessarily limited: topic is not the actual outcome $Y$, and small per-topic samples reduce statistical power. The design argument (LLM input excludes $Y$) remains the primary justification; the stress test in Section~\ref{sec:nondiff_stress} below quantifies bias degradation under hypothetical violations.

\subsection{Non-Differential Assumption Stress Test}
\label{sec:nondiff_stress}

Our main analysis assumes non-differential misclassification: the confusion matrix $C$ does not depend on the outcome $Y$. To assess sensitivity to violations of this assumption, we conduct a stress test on the exposure topology ($K{=}3$, $\min_k C_{kk} \geq 0.7$, 10{,}000 random matrices per $\varepsilon$). For each base matrix $C_0$, observations with $Y > \text{median}(Y)$ are misclassified using a perturbed matrix $\tilde{C} = \text{renormalize}(C_0 + \varepsilon \cdot \Delta)$ where $\Delta_{jk} \sim \text{Uniform}(-1,1)$ for $j \neq k$, while observations with $Y \leq \text{median}(Y)$ retain $C_0$.

\begin{table}[h]
\centering
\small
\caption{Effect of differential misclassification on bias violation rates (exposure DAG, $K{=}3$, $\min_k C_{kk} \geq 0.7$, $N{=}50{,}000$ MC samples per trial).}
\label{tab:nondiff_stress}
\begin{tabular}{cccc}
\toprule
$\varepsilon$ & Sign-Flip (\%) & Amplification (\%) & Any Violation (\%) \\
\midrule
0.00 & 2.0 & 11.5 & 13.3 \\
0.01 & 2.4 & 12.4 & 14.7 \\
0.05 & 2.8 & 13.0 & 15.7 \\
0.10 & 4.5 & 16.7 & 20.9 \\
0.20 & 9.7 & 23.7 & 32.6 \\
0.30 & 15.5 & 28.7 & 42.8 \\
0.50 & 22.9 & 34.9 & 54.6 \\
\bottomrule
\end{tabular}
\end{table}

The $\varepsilon{=}0$ baseline (13.3\% violation) closely matches the analytical result (13.7\%) from Section~\ref{sec:taxonomy_results}. Small departures ($\varepsilon \leq 0.05$) change rates by fewer than 3 percentage points. At $\varepsilon{=}0.10$, the violation rate increases by 7.6~pp; at $\varepsilon{=}0.50$, it rises to 54.6\% (+41.2~pp), indicating substantial sensitivity to large differential error. Our main conclusions hold under mild violations but should be interpreted cautiously when outcome-conditional annotation is suspected.

\paragraph{Extended stress test.}
An extended stress test across 96 configurations (4 topologies $\times$ 2 $K$ $\times$ 3 CM qualities $\times$ 4 $\varepsilon$, 100 MC replications each; Table~\ref{tab:stress-test}) confirms these patterns broadly: at $\varepsilon{=}0$, sign-flip rates are zero for all configurations; at $\varepsilon{=}0.10$, the maximum sign-flip rate is 7\% (IV, low-quality $C_{kk}{=}0.4$, $K{=}3$). At $K{=}10$, sign-flip rates remain zero for all topologies and $\varepsilon$ values, suggesting that higher $K$ provides additional robustness to differential misclassification.

\input{exp_a_stress_test_table.tex}

\paragraph{Structured violation models.}
Beyond uniform perturbations, we compare two structured violation models across all seven topologies ($K{=}3$, 1{,}000 matrices per $\varepsilon$, $\min_k C_{kk}{\geq}0.7$): (1)~\emph{outcome-conditional}, where $\tilde{C}$ applies to $Y{>}\text{median}(Y)$ while $C_0$ applies below; (2)~\emph{label-specific}, where off-diagonal perturbation magnitude scales with $|\text{corr}(A{=}k, Y)|$, modeling accuracy degradation on $Y$-predictive categories. For exposure at $\varepsilon{=}0.20$, label-specific violations are substantially milder than uniform (sign-flip 0.1\% vs.\ 10.1\%), while outcome-conditional violations are more severe (sign-flip 13.9\%, mean relative bias 0.50 vs.\ 0.40). All five sign-preserving topologies maintain 0\% sign-flip under both structured models at all $\varepsilon$ tested; IV likewise shows 0\% sign-flip (its vulnerability requires pathological diagonal structure, not differential error). These results indicate that the uniform perturbation model is neither uniformly conservative nor uniformly liberal: practitioners should assess which violation pattern is most plausible for their annotation pipeline.

\subsection{Robustness to Nonlinear DGP}
\label{sec:logistic_dgp}

We replace the linear structural equations with $Y \sim \text{Bernoulli}(\sigma(\mathbf{x}^\top\boldsymbol{\beta}))$ and estimate via logistic regression (control function for IV) for all seven topologies, using $K{=}3$, six real VAST confusion matrices, 500 MC replications, and $N{=}10{,}000$. Mean sign-flip rates across the six CMs:

\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
Topology & Linear SF$_\tau$ & Logistic SF$_\tau$ & Linear SF$_\text{any}$ & Logistic SF$_\text{any}$ \\
\midrule
Confounding & 0.000 & 0.000 & 0.573 & 0.559 \\
Mediation & 0.000 & 0.000 & 0.703 & 0.695 \\
Collider & 0.000 & 0.000 & 0.060 & 0.060 \\
Exposure & --- & --- & 0.577 & 0.574 \\
M-bias & 0.000 & 0.000 & 0.095 & 0.099 \\
Front-door & 0.000 & 0.000 & 0.590 & 0.579 \\
IV & 0.086 & 0.074 & 0.086 & 0.560 \\
\bottomrule
\end{tabular}
\end{center}

Treatment-effect sign-flip rates (SF$_\tau$) are zero for all five guaranteed topologies under both linear and logistic DGPs, and differ by $< 0.02$ for IV. The SF$_\text{any}$ discrepancy for IV reflects estimator dimensionality: the control function includes a residual coefficient prone to sign noise; the core comparison is SF$_\tau$, where both estimators agree. Linear and logistic DGPs produce virtually identical sign-flip patterns across all seven topologies ($|\Delta\,\text{SF}_\tau| < 0.02$). Under logistic DGP, bias is measured in log-odds units; directional conclusions (sign preservation, topology ranking) transfer directly, while magnitudes are not directly comparable to linear plim expressions. For practitioners requiring finite-sample inference under logistic DGP, bias-corrected bootstrap provides a nonparametric alternative that bypasses the linear plim framework.

\subsection{Bootstrap Uncertainty of ASV}
\label{sec:bootstrap_asv}

We assess confusion-matrix estimation uncertainty by bootstrapping label pairs from HumAID DeepSeek zero-shot ($K{=}10$, $n{=}1{,}500$): resample with replacement $B{=}1{,}000$ times, re-estimate $C_0$, and recompute magnitude ASV $\delta^*$ at a 10\% bias threshold.

\begin{center}
\small
\begin{tabular}{lccc}
\toprule
Topology & $\delta^*$ (point) & 95\% CI & Valid / 1000 \\
\midrule
Confounding & --- & --- & 0 \\
Exposure & 0.937 & [0.602, 1.363] & 907 \\
IV & 0.754 & [0.435, 1.290] & 902 \\
\bottomrule
\end{tabular}
\end{center}

Confounding never reaches the 10\% threshold across all 1{,}000 bootstraps, confirming robustness. For IV, the 95\% CI spans a $3{\times}$ range (0.435 to 1.290); practitioners should bootstrap $C_0$ rather than rely on a point estimate when ASV is near the decision boundary. CI width scales as $O(1/\sqrt{n})$ where $n$ is the gold-label validation set size; at $n{=}1{,}500$, IV CIs are ${\sim}3{\times}$ wider than exposure CIs, reflecting the Wald estimator's denominator instability. A convergence study ($K{=}3$, $B{=}1{,}000$) confirms this scaling: exposure CI width narrows from 0.60 ($n{=}500$) to 0.05 ($n{=}50{,}000$), and IV from 0.52 to 0.04, with the product $\text{width} \times \sqrt{n}$ approximately constant (CV $< 0.20$).

\begin{figure}[h]
\centering
\includegraphics[width=0.9\columnwidth]{exp_c_bootstrap_figure.pdf}
\caption{Bootstrap distribution of magnitude ASV $\delta^*$ ($B{=}1{,}000$) for HumAID DeepSeek zero-shot ($K{=}10$). Dashed line: point estimate; shaded: 95\% CI.}
\label{fig:bootstrap_asv}
\end{figure}

\subsection{Cross-$K$ Robustness}
\label{sec:cross_k_robustness}

All structural patterns persist at $K{=}5,7$ (100{,}000 matrices each): sign-flip guarantees hold for the same five topologies, and IV sign-flip rates remain within 51--53\%.

\subsection{ASV Diagnostic Results}
\label{sec:asv_diagnostics}

For HumAID ($K{=}10$, 56 scenarios), 55.4\% are flagged as magnitude-ASV-unsafe---IV, mediation, front-door, and exposure all trigger, while confounding, collider, and M-bias remain below 10\% bias (Figure~\ref{fig:asv_heatmap}). For CivilComments ($K{=}2$), 100\% of scenarios are ASV-unsafe despite $F_1$ ranging from $0.69$ (7B) to $0.82$ (frontier).

Overlaying all 23 empirical matrices onto the theoretical Dirichlet violation-rate curves (Figure~\ref{fig:dirichlet_overlay}) confirms placement: VAST in the danger zone, HumAID mid-range, CivilComments approaching safe for exposure but still in IV amplification.

\subsection{ASV Stability Across DGP Parameters}
\label{sec:asv_stability}

We test whether ASV rankings are stable across different data-generating process (DGP) configurations by varying the treatment-effect vector $\boldsymbol{\beta}$ across three settings: alternating-large ($\boldsymbol{\beta}=[0,\,1.0,\,-0.5]$), same-sign ($\boldsymbol{\beta}=[0,\,0.5,\,0.3]$), and alternating-small ($\boldsymbol{\beta}=[0,\,0.2,\,-0.1]$), with $K{=}3$, $\tau{=}0.5$, and five $C_0$ diagonal values $\in\{0.5,0.6,0.7,0.8,0.9\}$.

\input{exp_e_asv_stability_table.tex}

Three patterns emerge (Table~\ref{tab:asv_stability}). First, IV magnitude ASV is identical across all three DGPs ($\delta^*{=}0.203$). This is a mathematical identity, not an empirical finding: the Wald estimator $\hat{\tau}_{\text{IV}} = \text{RF}/\text{FS}$ has $\boldsymbol{\beta}$ in both numerator and denominator, and the ratio depends only on $C$ and the first-stage structure, making ASV invariant to $\boldsymbol{\beta}$ by construction. Second, exposure ASV depends on the coefficient ratio structure: alternating-large ($\beta_2/\beta_1 = -0.5$) and alternating-small ($\beta_2/\beta_1 = -0.5$) share the same ratio and yield identical $\delta^*{=}0.090$, while same-sign ($\beta_2/\beta_1 = 0.6$) gives $\delta^*{=}0.223$---thus the experiment provides two distinct sign-structure conditions, not three. Third, confounding ASV depends on effect magnitudes: only alternating-large triggers the 10\% threshold ($\delta^*{=}0.348$); smaller effects never reach 10\% bias.

Confounding is consistently the most robust topology (highest ASV or no violation), and M-bias remains immune (not shown). However, the relative fragility of exposure vs.\ IV inverts with coefficient sign structure: under alternating signs, exposure is most fragile ($0.090 < 0.203$); under same-sign effects, IV is most fragile ($0.203 < 0.223$). Practitioners should compute ASV with their specific DGP rather than relying on generic rankings.

\subsection{Extended Conjecture Verification}
\label{sec:conjecture_verify}

We test envelope monotonicity (Conjecture~\ref{conj:monotone}) by sampling 10{,}000 random $C_0$ matrices from Dirichlet$(1,\dots,1)$ per topology for $K \in \{3,5,7\}$ (210{,}000 total), sweeping $\delta$ in steps of 0.01, and counting matrices with at least one non-monotonic step exceeding tolerance $10^{-12}$. Bias at each $\delta$ is computed via analytic probability limits using Gauss--Hermite quadrature (100 points), eliminating sampling noise entirely.

\begin{table}[h]
\centering
\small
\caption{Envelope monotonicity violations per 10{,}000 random $C_0$ matrices ($\delta$ step $= 0.01$). ``Worst mag.''\ is the largest single-step deviation $|B(\delta_{i+1})|-|B(\delta_i)|$ in the non-monotonic direction. $^*$100{,}000 matrices at step $= 0.005$ yield exposure $\varepsilon = 0.075$.}
\label{tab:conjecture_verify}
\begin{tabular}{lccccc}
\toprule
\textbf{Topology} & $K{=}3$ & $K{=}5$ & $K{=}7$ & \textbf{Worst mag.} & \textbf{Direction} \\
\midrule
Confounding & 8467 & 8063 & 7892 & 0.011 & non-dec \\
Mediation   & 8236 & 7905 & 7893 & 0.013 & non-dec \\
Collider    & 8683 & 7669 & 7570 & 0.005 & non-inc \\
Exposure    & 485  & 205  & 77   & 0.056$^*$ & non-dec \\
M-bias      & 9791 & 7783 & 7399 & 0.002 & non-inc \\
IV          & 7433 & 6015 & 5711 & $>10^6$ & non-dec \\
Front-door  & 8429 & 8079 & 7899 & 0.013 & non-dec \\
\bottomrule
\end{tabular}
\end{table}

Violation counts are high (60--97\% of matrices), but magnitudes are small for non-IV topologies under the default parameterization (Table~\ref{tab:conjecture_verify}); DGP sensitivity analysis (Table~\ref{tab:dgp_sensitivity}) shows $\varepsilon$ scales with effect magnitude $|\beta|$, remaining below 0.02 for $|\beta| \leq 1$ across non-exposure topologies. These results are computed from analytic probability limits with zero sampling noise, confirming the violations are structural properties of multi-class confusion matrices rather than numerical artifacts. IV deviations reflect Wald estimator singularities when the first-stage coefficient passes near zero---this is a property of the Wald estimator, not of the ASV parameterization. Adversarial search (500 restarts per configuration, $K \in \{3,5\}$) finds genuine counterexamples for all topologies except IV $K{=}3$, confirming that strict monotonicity does not hold universally. The bias envelope is $\varepsilon$-approximately monotonic with $\varepsilon < 0.03$ for collider and M-bias across all tested $\tau$; confounding, mediation, and front-door achieve $\varepsilon < 0.06$ at default DGP parameters (see DGP sensitivity below); exposure satisfies $\varepsilon_{p99} < 0.008$ but extreme-tail matrices ($<$0.02\% of draws) reach $\varepsilon \approx 0.15$. Empirically, $\varepsilon$ scales as $O(1/K^\alpha)$ with $\alpha \in [0.6, 1.5]$ (topology-dependent): for confounding, mediation, and front-door, $\alpha \approx 1.4$; for exposure, $\alpha \approx 1.0$; for collider and M-bias, $\alpha \approx 0.7$. A universal empirical upper bound is $\varepsilon \leq 0.137/\sqrt{K}$, verified across all tested configurations ($K \in \{3,5,7\}$). This scaling supports the practical validity of ASV as a sensitivity tool at any meaningful bias threshold.

\paragraph{DGP parameter sensitivity.}
We repeat the verification across $\tau \in \{0.1, 0.5, 1.0, 2.0, 5.0\}$, varying the direct $T{\to}Y$ coefficient for confounding ($\beta_T{=}\tau$), mediation, collider, and M-bias (DGP $\tau$), and rescaling the $A{\to}Y$ coefficient vector to $\boldsymbol{\beta}\cdot\tau$ for exposure and front-door. With 10{,}000 random $C_0$ per $(K,\tau)$ setting (Table~\ref{tab:conjecture_tau_sens}), $\varepsilon$ remains small ($< 0.03$) for $\tau \geq 0.5$ for confounding, mediation, collider, and M-bias, but grows at $\tau{=}0.1$: mediation produces 1{,}950 / 30{,}000 violations across $K \in \{3,5,7\}$ (worst $\varepsilon{=}0.130$ at $K{=}3$) and confounding 200 / 30{,}000 (worst $\varepsilon{=}0.118$ at $K{=}3$). The mechanism is dimensional: additive terms not scaling with $\tau$ (e.g.\ the $\boldsymbol{\beta}$-contribution to $\mathbb{E}[Y D_k]$ in mediation and confounding) contribute a $\tau$-independent offset to the bias numerator, so the ratio $|B|/|\tau|$ inflates as $\tau \to 0$. A single mediation violation at $\tau{=}2.0$ ($\varepsilon{=}0.112$, 1/30{,}000 matrices) arises from a specific confusion matrix structure where off-diagonal entries induce partial cancellation along the mediation pathway; this isolated case does not affect the $p_{99}$ bound ($\varepsilon_{p99}{=}0.005$). Collider and M-bias remain robust at all $\tau$ ($\varepsilon{<}0.022$ and $\varepsilon{<}0.015$ respectively). Exposure shows scattered tail outliers but $\overline{\varepsilon} \approx 3\!\times\!10^{-4}$ at every $\tau$, consistent with linear scaling: when $\boldsymbol{\beta}$ rescales by $\tau$, both bias and $\tau_{\text{true}}$ scale by the same factor and $\varepsilon$ is theoretically invariant. Front-door inverts the pattern ($\varepsilon$ \emph{grows} with $\tau$, reaching $0.139$ at $\tau{=}5$, $K{=}3$, with 664 violations) because $\tau_{\text{true}} = \operatorname{plim}(\hat{\tau}_T \mid C{=}I)$ is pinned at $0.5$ by the $U$-confounding path while bias magnitudes scale with $\boldsymbol{\beta}$---this is an artefact of how $\tau_{\text{true}}$ is normalised, not a genuine failure of envelope monotonicity. In practice, ASV's $\delta^*$ thresholds (typically $\geq 0.1$ in our deployment) are well above the region where these $\varepsilon$-violations occur, so the small-$\tau$ degradation does not affect ASV's operational reliability; we flag it as a caveat for applications targeting $|\tau| \lesssim 0.1$ in standardised units.

\begin{table}[h]
\centering
\small
\caption{$\varepsilon \equiv \max_\delta(|B(\delta{+}\Delta)|{-}|B(\delta)|)/|\tau_{\text{true}}|$ in the non-monotonic direction (max over $K \in \{3,5,7\}$; 10{,}000 random $C_0$ per cell). Bold marks $\varepsilon > 0.02$ (non-exposure) or $\varepsilon > 0.08$ (exposure). Counts in parentheses are violations / 30{,}000. Tables~\ref{tab:conjecture_verify} and~\ref{tab:conjecture_tau_sens} use independent random seeds; for topologies with low violation rates (exposure $<$0.02\%), the worst-case $\varepsilon$ is sample-sensitive.}
\label{tab:conjecture_tau_sens}
\begin{tabular}{lccccc}
\toprule
\textbf{Topology} & $\tau{=}0.1$ & $\tau{=}0.5$ & $\tau{=}1.0$ & $\tau{=}2.0$ & $\tau{=}5.0$ \\
\midrule
Confounding & \textbf{0.118} (200) & 0.027 (0) & 0.014 (0) & 0.007 (0) & 0.002 (0) \\
Mediation   & \textbf{0.130} (1950) & 0.026 (0) & 0.013 (0) & \textbf{0.112} (1) & 0.003 (0) \\
Collider    & 0.022 (0) & 0.006 (0) & 0.005 (0) & 0.004 (0) & 0.002 (0) \\
Exposure    & \textbf{0.111} (5) & \textbf{0.122} (2) & \textbf{0.152} (2) & \textbf{0.089} (3) & \textbf{0.148} (3) \\
M-bias      & 0.014 (0) & 0.003 (0) & 0.001 (0) & 0.001 (0) & 0.000 (0) \\
Front-door  & 0.003 (0) & 0.013 (0) & 0.027 (0) & 0.051 (0) & \textbf{0.139} (664) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Effect-magnitude sensitivity.}
We further vary the direct effect $\beta \in \{0.1, 0.5, 1.0, 2.0, 5.0\}$ and noise variance $\sigma^2 \in \{0.5, 1.0, 2.0\}$ across all seven topologies ($K{=}3$, 10{,}000 Dirichlet$(1,\ldots,1)$ matrices per cell). Table~\ref{tab:dgp_sensitivity} reports the worst-case relative envelope deviation $\varepsilon_{\text{rel}}$. For non-exposure topologies, $\varepsilon$ scales approximately linearly with $|\beta|$; the safe/fragile partition is stable across all 105 cells.

\input{conjecture_dgp_sensitivity_table.tex}

\subsection{Proportional Effects Breakdown}
\label{sec:proportional_effects}

Theorem~\ref{thm:monotone} requires proportional effects ($f = \lambda g$); here we quantify how $\lambda(C)$ out-of-bounds (OOB) rates depend on the degree of departure from proportionality. We simulate $K{=}3$ mediation with 100{,}000 random confusion matrices (diagonal $\geq 0.5$, Dirichlet off-diagonal) at five proportionality degrees: exact ($f_j {-} f_0 = \lambda(g_j {-} g_0)$), near-proportional ($f = \lambda g + \epsilon$, $\epsilon \sim \mathcal{N}(0,\sigma^2)$, $\sigma \in \{0.1, 0.3, 0.5\}$), and fully independent ($f \perp g$). Table~\ref{tab:proportional_effects} reports the $\lambda(C) \notin [0,1]$ rate.

\begin{table}[h]
\centering
\small
\caption{$\lambda(C)$ out-of-bounds rate and mediation sign failure rate by degree of departure from proportional effects ($K{=}3$, 100K random confusion matrices per row).}
\label{tab:proportional_effects}
\begin{tabular}{lcc}
\toprule
\textbf{Proportionality degree} & $\lambda(C) \notin [0,1]$ & Sign failure \\
\midrule
Exact ($\sigma{=}0$) & 0.0\% & 0.85\% \\
Near ($\sigma{=}0.1$) & 10.3\% & 0.87\% \\
Near ($\sigma{=}0.3$) & 19.0\% & 0.96\% \\
Near ($\sigma{=}0.5$) & 22.4\% & 0.93\% \\
Independent ($f \perp g$) & 22.9\% & 0.17\% \\
\midrule
NLP-realistic (plan\_006 DGP) & 0.07\% & 0.0\% \\
\bottomrule
\end{tabular}
\end{table}

The $\lambda$ OOB rate rises steeply from 0\% to ${\sim}10$\% at $\sigma{=}0.1$ and saturates near 23\% for independent $(f,g)$. Sign failure rates remain below 1\% across all conditions because $\lambda$ excursions outside $[0,1]$ are typically small and do not reverse the dominant effect direction. The NLP-realistic DGP ($\delta{=}[0, 0.8, {-}0.6]$, $\beta{=}[0, 1.0, {-}0.5]$) exhibits near-zero OOB (0.07\%) and zero sign failures, confirming that proportional effects hold approximately in practice.

\subsection{FWL Invariance Under Nonlinear Models}
\label{sec:fwl_nonlinear}

FWL invariance guarantees exactly 0\% bias reduction from matrix-based corrections (MLA, naive plug-in) under linear OLS for covariate topologies (confounding, mediation, collider, M-bias). Here we test whether this invariance persists under logistic regression across sample sizes $N \in \{1000, 5000, 10{,}000, 50{,}000\}$ and three error levels ($C_{kk} \in \{0.9, 0.7, 0.5\}$), with 200 Monte Carlo replicates per cell. Table~\ref{tab:fwl_nonlinear} reports the mean logistic bias reduction (positive = correction helps).

\begin{table}[h]
\centering
\small
\caption{Mean logistic bias reduction (\%) from matrix-based correction, by topology and error level (averaged over $N \in \{1K, 5K, 10K, 50K\}$; 200 MC reps each). Under linear OLS, all covariate topologies yield exactly 0\%. Exposure is shown for comparison.}
\label{tab:fwl_nonlinear}
\begin{tabular}{lccc}
\toprule
\textbf{Topology} & \textbf{Low} ($C_{kk}{=}0.9$) & \textbf{Med} ($C_{kk}{=}0.7$) & \textbf{High} ($C_{kk}{=}0.5$) \\
\midrule
Confounding & $-$0.003\% & $<$0.001\% & $<$0.001\% \\
Mediation & $-$0.004\% & $<$0.001\% & $<$0.001\% \\
Collider & $-$0.033\% & $-$0.020\% & $<$0.001\% \\
M-bias & $-$0.049\% & $-$0.010\% & $<$0.001\% \\
Front-door & 0.001\% & $-$0.001\% & $<$0.001\% \\
\midrule
Exposure & $-$68.5\% & $-$48.7\% & $-$22.5\% \\
\bottomrule
\end{tabular}
\end{table}

For all five covariate topologies, logistic bias reduction is negligible ($|$mean$| < 0.05\%$) across all $N$ and error levels, with no systematic $N$-scaling: medians cluster within $\pm 0.002\%$ at $N{=}50{,}000$. The large standard deviations observed at certain $(N, \text{error})$ cells for collider and M-bias (up to 2.6\% std at $N{=}10{,}000$) reflect rare outlier replicates; medians remain near zero. This confirms that FWL invariance is a robust structural property that extends beyond linear models for covariate topologies.

In contrast, exposure shows substantial FWL breakdown under logistic regression: $-$22\% to $-$78\% bias reduction (negative = correction worsens bias), stabilising at $N \geq 5{,}000$. The breakdown magnitude increases with accuracy ($C_{kk}$), consistent with the exposure topology's sensitivity to the nonlinear link function transforming category weights.

\subsection{PPI++ Gold Label Ratio Sensitivity}
\label{sec:ppi_gold_ratio}

We test PPI++ correction performance as a function of gold label fraction ($K{=}3$, $N{=}10{,}000$, 100 MC replications, 5 Dirichlet confusion matrices with $\min_k C_{kk}{\geq}0.6$).

\begin{table}[h]
\centering
\small
\caption{PPI++ RMSE reduction (\%) vs.\ gold label fraction across five topologies. Negative values indicate PPI++ increases RMSE relative to the naive estimator.}
\label{tab:ppi_gold_ratio}
\begin{tabular}{lccccc}
\toprule
Topology & 5\% & 10\% & 20\% & 50\% & 100\% \\
\midrule
Confounding & $+$84.8 & $+$88.6 & $+$91.8 & $+$94.2 & $+$95.2 \\
Exposure & $+$75.2 & $+$83.1 & $+$87.1 & $+$91.2 & $+$93.8 \\
Mediation & $+$56.7 & $+$70.4 & $+$77.2 & $+$85.3 & $+$88.7 \\
IV & $+$1.4 & $+$22.1 & $+$50.1 & $+$66.7 & $+$76.4 \\
M-bias & $-$125.3 & $-$43.6 & $-$19.6 & $+$6.0 & $+$18.5 \\
\bottomrule
\end{tabular}
\end{table}

For confounding and exposure, PPI++ achieves $>$75\% RMSE reduction even at 5\% gold labels; IV requires ${\geq}20\%$ gold to reach 50\% reduction, reflecting the Wald estimator's higher variance. M-bias exhibits harmful correction at ${\leq}20\%$ gold ($-125\%$ at 5\%), because PPI++ re-exposes the M-bias pathway that annotation error had attenuated; only at ${\geq}50\%$ gold does the estimator become weakly beneficial ($+6\%$).

\subsection{Realistic Differential Misclassification Thresholds}
\label{sec:realistic_epsilon}

Our non-differential assumption (Definition~\ref{def:nondiff}) requires $P(A^*{\mid}A,Y) = P(A^*{\mid}A)$. We estimate how much $Y$-stratified differential error each of our 23 empirical LLM confusion matrices can tolerate before exposure sign reversal exceeds 5\%. For each matrix $C$, we construct $C_{\text{high}} = \text{renorm}(C + \varepsilon\Delta)$ and $C_{\text{low}} = \text{renorm}(C - \varepsilon\Delta)$ with random off-diagonal perturbation $\Delta$ (500 MC trials per $\varepsilon$), sweep $\varepsilon \in [0, 0.50]$, and report the safe threshold $\varepsilon^*_{\text{safe}} = \max\{\varepsilon : \text{reversal rate} < 5\%\}$ (Table~\ref{tab:realistic_epsilon}).

\begin{table}[h]
\centering
\small
\caption{Safe differential misclassification thresholds $\varepsilon^*_{\text{safe}}$ by dataset/K. For $K{=}2$ (binary), no configuration produces sign reversal up to $\varepsilon{=}0.50$. For $K{\geq}3$, the majority of configurations are fragile ($\varepsilon^*_{\text{safe}}{=}0$), confirming that the non-differential assumption is load-bearing for multi-class exposure.}
\label{tab:realistic_epsilon}
\begin{tabular}{lccccc}
\toprule
\textbf{Dataset} & $K$ & $n$ & \textbf{Mean} $\varepsilon^*$ & \textbf{Median} $\varepsilon^*$ & $\varepsilon^*{=}0$ \\
\midrule
Civil Comments & 2 & 7 & 0.50 & 0.50 & 0/7 \\
VAST & 3 & 8 & 0.003 & 0.00 & 7/8 \\
HuMAID & 10 & 8 & 0.045 & 0.00 & 7/8 \\
\bottomrule
\end{tabular}
\end{table}

The distribution is sharply bimodal: all $K{=}2$ configurations are immune to differential misclassification within the tested range ($\varepsilon^*_{\text{safe}}{=}0.50$), consistent with the binary sign-preservation guarantee (Proposition~\ref{prop:signflip}(c)). For $K{\geq}3$, 14 of 16 configurations have $\varepsilon^*_{\text{safe}}{=}0$---sign reversal occurs at the smallest tested $\varepsilon{=}0.01$. Two outliers (VAST: $\varepsilon^*{=}0.02$; HuMAID: $\varepsilon^*{=}0.36$) have atypically structured confusion matrices. This confirms that even small violations of non-differential misclassification are consequential for multi-class exposure, underscoring the practical importance of validating $P(A^*{\mid}A,Y) \approx P(A^*{\mid}A)$ on gold-labeled subsets when $K \geq 3$.

\subsection{Proportional Effects with Empirical Confusion Matrices}
\label{sec:proportional_real_matrices}

Theorem~\ref{thm:monotone} assumes proportional effects ($f = \lambda g$). We evaluate this assumption using all 23 empirical LLM confusion matrices under four DGP groups in the mediation topology (23 matrices $\times$ 50 seeds $\times$ 100{,}000 precomputed samples per seed).

\begin{table}[h]
\centering
\small
\caption{Proportional effects validation with 23 real LLM confusion matrices under mediation DGP. $\lambda(C) \in [0,1]$: proportion satisfying the Theorem~\ref{thm:monotone} bound. Sign pres.: sign preservation rate of the direct effect estimate.}
\label{tab:proportional_empirical}
\begin{tabular}{lcccc}
\toprule
\textbf{DGP Group} & $\lambda(C)$ \textbf{mean} & $\lambda(C) \in [0,1]$ & \textbf{Sign pres.} & \textbf{CV} \\
\midrule
Proportional ($f{=}\lambda g$) & 0.271 & 99.8\% & 100\% & 0.00 \\
Near-prop.\ ($\sigma{=}0.1$) & 0.285 & 99.2\% & 100\% & 1.06 \\
Non-proportional & 0.240 & 85.0\% & 97.3\% & 3.60 \\
Sparse & 0.296 & 75.8\% & 98.6\% & 6.59 \\
\bottomrule
\end{tabular}
\end{table}

Under proportional and near-proportional effects, $\lambda(C)$ remains within $[0,1]$ for ${\geq}99\%$ of configurations with 100\% sign preservation---the Theorem~\ref{thm:monotone} conditions are satisfied by real LLM confusion matrices. Even under non-proportional effects ($f$ independent of $g$), sign preservation remains at 97.3\%, with $\lambda(C)$ exceeding $[0,1]$ in 15\% of cases. The per-$K$ breakdown shows that $\lambda(C)$ is dimension-dependent: $K{=}2$ (mean 0.249), $K{=}3$ (mean 0.082), $K{=}10$ (mean 0.490), with 100\% in $[0,1]$ across all $K$ under proportional effects. Sparse DGP (zero-effect categories) produces the highest violation rate (24.2\%), consistent with the breakdown mechanism: zero-effect categories make $f/g$ ratios undefined, maximally departing from proportionality.

\subsection{Case Study: ASV Diagnosis of Political Ad Tone Classification}
\label{sec:asv_case_study}

We illustrate the ASV diagnostic workflow on a concrete CSS application. \citet{egami_hinck_stewart_wei2023} use LLM classifiers to annotate political advertising tone ($K{=}3$: promote/contrast/attack) from the Fowler \& Ridout database, then estimate the causal effect of ad tone on voter outcomes. The annotated variable is the \textbf{exposure} (treatment). Using a representative $K{=}3$ confusion matrix from our empirical set (VAST DeepSeek zero-shot, mean diagonal 0.734) as a proxy:

\begin{center}
\small
\begin{tabular}{lccc}
\toprule
Coefficient & $\delta^*_{\text{mag}}$ (10\%) & $\delta^*_{\text{sf}}$ & Relative bias at $\delta{=}1$ \\
\midrule
$\beta_1$ (primary) & 0.234 & $> 1.5$ & 39.7\% \\
$\beta_2$ (secondary) & 0.154 & $> 1.5$ & 60.4\% \\
\bottomrule
\end{tabular}
\end{center}

Both coefficients are magnitude-unsafe ($\delta^* < 1$), with the secondary coefficient more fragile ($\delta^*{=}0.154$). If the same annotator were instead classifying a \textbf{confounder} (e.g., media framing as a control variable), magnitude ASV rises to $\delta^*{=}0.690$---a $3{\times}$ increase in error tolerance from the same confusion matrix. This demonstrates the central insight: topology, not annotator quality, determines vulnerability. Algorithm~\ref{alg:correction} routes this exposure topology to PPI++ or DSL correction; matrix-based methods (naive plug-in, MLA) are ineffective due to FWL non-applicability.

\subsection{End-to-End Demonstration}
\label{sec:e2e_demo}

\input{e2e_demo_table.tex}

The demonstration uses a symmetric $C_0$ ($d_{\mathrm{rel}}{=}0$); empirical $K{=}3$ matrices deviate substantially (median $d_{\mathrm{rel}}{=}0.33$, Remark~\ref{rem:symmetry}), but core ASV ranking and correction routing remain valid under Conjecture~\ref{conj:monotone}.

\subsection{Semi-Real Case Study with Production LLM Confusion Matrices}
\label{sec:real_cm_demo}

We repeat the end-to-end pipeline using 10 confusion matrices estimated from production LLM annotation of VAST stance detection ($K{=}3$; DeepSeek, Llama4, Qwen2.5-7B, Qwen3 under zero-shot and few-shot prompting). These matrices exhibit median $d_{\mathrm{rel}}{=}0.33$ (Remark~\ref{rem:symmetry}), far from the symmetric assumption of Theorem~\ref{thm:k3_symmetric}. Results confirm both topology-dependent patterns: PPI++ achieves 93.6\% median exposure bias reduction while plug-in corrections yield 0\% improvement for confounding (FWL invariance, machine precision; Table~\ref{tab:e2e-real-cm}).

\input{e2e_real_cm_table.tex}

\subsection{Correction Method Sensitivity to $K$}
\label{sec:correction_k_sensitivity}

We test whether the correction method ranking from Table~\ref{tab:correction} ($K{=}3$) is stable at $K{=}5$ and $K{=}10$. For each $K$, we sample 5 random Dirichlet confusion matrices (diagonal $\geq 0.6$), run 100 MC replications per topology--matrix combination ($N{=}5{,}000$, 10\% gold fraction), and compute RMSE reduction for all five methods across all seven topologies.

\paragraph{Grand ranking stability.}
Spearman rank correlations across all topology--method pairs: $\rho_{3,5} = 0.80$, $\rho_{3,10} = 0.84$, $\rho_{5,10} = 0.93$ (all $p < 10^{-8}$). Per-topology correlations are even stronger: 5 of 7 topologies (confounding, collider, M-bias, IV, and exposure at $K{=}10$) achieve $\rho = 1.0$ between $K{=}3$ and $K{=}10$.

\paragraph{Method-specific patterns.}
PPI++ effectiveness degrades with $K$: confounding RMSE reduction drops from 85.6\% ($K{=}3$) to 75.6\% ($K{=}10$); mediation drops from 57.1\% to 9.0\%. At $K{=}10$, PPI++ becomes harmful for IV ($-124\%$) and exposure ($-11\%$). MC-SIMEX is the most $K$-robust method, maintaining the top rank for 5/7 topologies at $K{=}10$ (vs.\ 3/7 at $K{=}3$). FWL invariance (naive plug-in and MLA achieving 0\% bias reduction on the treatment coefficient) holds at all $K$ tested, confirming the structural nature of this result.

\begin{table}[h]
\centering
\small
\caption{Top correction method and RMSE reduction (\%) by topology across $K$. Bold = top method for that topology--$K$ combination.}
\label{tab:correction_k}
\begin{tabular}{lccc}
\toprule
\textbf{Topology} & $K{=}3$ & $K{=}5$ & $K{=}10$ \\
\midrule
Confounding & \textbf{PPI++} 85.6 & \textbf{PPI++} 76.4 & \textbf{PPI++} 75.6 \\
Mediation & \textbf{PPI++} 57.1 & \textbf{SIMEX} 59.7 & \textbf{SIMEX} 59.0 \\
Collider & \textbf{SIMEX} 55.4 & \textbf{SIMEX} 63.4 & \textbf{SIMEX} 57.3 \\
Exposure & \textbf{SIMEX} 72.5 & \textbf{SIMEX} 65.0 & \textbf{SIMEX} 47.3 \\
M-bias & \textbf{SIMEX} 3.2 & \textbf{SIMEX} 47.0 & \textbf{SIMEX} 43.7 \\
IV & \textbf{SIMEX} 30.9 & \textbf{SIMEX} 30.4 & \textbf{SIMEX} 27.1 \\
Front-door & \textbf{PPI++} 82.9 & \textbf{PPI++} 69.0 & \textbf{SIMEX} 70.1 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comparison with Related Sensitivity Frameworks}
\label{sec:sensitivity_comparison}

\begin{table}[h]
\centering
\caption{Comparison of sensitivity frameworks.}
\label{tab:sensitivity_comparison}
\small
\begin{tabular}{lccc}
\toprule
& \textbf{ASV} & \textbf{E-value} & \textbf{Partial} $R^2$ \\
\midrule
Parameter space & $K{\times}K$ confusion $C(\delta)$ & Scalar confounder & $R^2$ of omitted var \\
Sensitivity path & $(1{-}\delta)I + \delta C_0$ & Monotone scalar & Monotone scalar \\
Topology-specific & \checkmark & \ding{55} & \ding{55} \\
Sign-flip diagnostic & \checkmark ($\delta^*_{\text{sign}}$) & \ding{55} & \ding{55} \\
Topology dependence & \checkmark (7 topologies ranked) & \ding{55} & \ding{55} \\
Monotonicity & Proved $K{=}2$; conj.\ $K{\geq}3$ & Guaranteed & Guaranteed \\
Target & Annotation error & Unmeasured confounding & Omitted variable \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Per-Topology $F_1$--Bias Correlation}
\label{sec:within_topo_f1}

The cross-topology correlation between macro $F_1$ and $|\text{bias}|$ is weak ($\rho = -0.10$, $p = 0.45$; \S\ref{sec:empirical}), but decomposing by topology reveals a structured pattern. Table~\ref{tab:within_topo_f1} reports Spearman correlations computed over 23 LLM configurations (HumAID $K{=}10$, VAST $K{=}3$, CivilComments $K{=}2$; 161 total scenarios).

\begin{table}[h]
\centering
\small
\caption{Within-topology Spearman $\rho(F_1, |\text{bias}|)$ across 23 empirical LLM configurations $\times$ 3 datasets. Negative $\rho$: higher $F_1$ $\to$ lower bias (expected). Positive $\rho$: higher $F_1$ $\to$ higher bias (protective attenuation removed).}
\label{tab:within_topo_f1}
\begin{tabular}{lccc}
\toprule
\textbf{Topology} & $n$ & $\rho$ & $p$ \\
\midrule
Confounding & 23 & $-0.557$ & 0.006 \\
Mediation & 23 & $-0.565$ & 0.005 \\
Exposure & 23 & $-0.531$ & 0.009 \\
Front-door & 23 & $-0.568$ & 0.005 \\
IV & 23 & $-0.700$ & $<$0.001 \\
Collider & 23 & $+0.310$ & 0.150 \\
M-bias & 23 & $+0.867$ & $<$0.001 \\
\bottomrule
\end{tabular}
\end{table}

Five topologies show the expected negative correlation: better annotation quality ($F_1{\uparrow}$) reduces causal bias. IV shows the strongest effect ($\rho = -0.70$), consistent with the Wald estimator's sensitivity to misclassification in the first stage. M-bias uniquely reverses: $\rho = +0.87$ ($p < 0.001$). The mechanism is that misclassification attenuates the spurious M-bias path, so the naive estimator under noisy annotations is \emph{closer} to the true causal effect; improving annotation quality (higher $F_1$) removes this protective attenuation and \emph{increases} bias. This is consistent with the correction results (\S\ref{sec:correction}), where PPI++ harms M-bias ($-$88\%) by removing the same protective noise. Collider shows a positive but non-significant trend ($\rho = +0.31$, $p = 0.15$), consistent with Berkson bias attenuation. Per-dataset breakdowns (CivilComments $K{=}2$: $\rho = -1.0$ for all topologies with $n{=}7$) should be interpreted with caution given the small sample and limited $F_1$ variation within the binary task.

\subsection{Confusion Matrix Estimation Robustness}
\label{sec:c_robustness}

The ASV framework requires the confusion matrix $C$, which in practice must be estimated from a held-out gold-labeled subset. We assess how estimation noise in $\hat{C}$ propagates to the safe/fragile partition and ASV ranking. For each of 8 empirical confusion matrices (VAST, 4 LLMs $\times$ 2 prompting strategies), we draw $n_{\text{gold}} \in \{50, 100, 200, 500\}$ samples from a $\text{Multinomial}(n_{\text{gold}}, C_{\cdot k})$ distribution per column, compute $\hat{C}$, and evaluate the resulting ASV ranking across 200 bootstrap replicates.

\begin{table}[h]
\centering
\small
\caption{$\hat{C}$ estimation robustness: safe/fragile partition stability and ASV ranking correlation (Spearman $\rho$ vs.\ true-$C$ ranking) under multinomial bootstrap from finite gold sets. 8 empirical matrices $\times$ 200 bootstrap replicates per $n_{\text{gold}}$.}
\label{tab:c_robustness}
\begin{tabular}{lccc}
\toprule
$n_{\text{gold}}$ & Partition stability & $\rho$ (median) & $\rho$ ($q_{25}$) \\
\midrule
50 & 50.4\% & 0.991 & 0.789 \\
100 & 82.0\% & 1.000 & 0.991 \\
200 & 85.6\% & 1.000 & 0.991 \\
500 & 88.5\% & 1.000 & 0.991 \\
\bottomrule
\end{tabular}
\end{table}

The ASV ranking is highly robust: median Spearman $\rho \geq 0.99$ even at $n_{\text{gold}}{=}50$. The safe/fragile partition (5 safe vs.\ 2 fragile topologies) recovers reliably from $n_{\text{gold}} \geq 100$ (82\% stability), increasing to 89\% at $n_{\text{gold}}{=}500$. The remaining instability concentrates in source matrices with low diagonal dominance (e.g., VAST DeepSeek few-shot: $\min C_{kk} = 0.16$), where bootstrap noise can shift borderline ASV values across the partition boundary. All 8 truth partitions match the canonical safe/fragile partition, confirming that the theoretical dichotomy holds empirically.

\subsection{Sign Reversal Determinants}
\label{sec:sign_determinants}

We investigate which confusion matrix features predict exposure sign reversal across the 23 empirical LLM configurations. For each matrix, we compute 10 structural features: $\min_k C_{kk}$ (minimum diagonal), $\overline{C_{kk}}$ (mean diagonal), $\max_{j \neq k} C_{jk}$ (maximum off-diagonal), $\max_{j \neq k} C_{jk}/C_{kk}$ (off-diagonal ratio), $\log(\kappa(C))$ (log condition number), mean and max column entropy, spectral gap, $\det(C)$, and minimum singular value. Sign reversal is defined as any exposure coefficient reversing sign under misclassification.

\begin{table}[h]
\centering
\small
\caption{Sign reversal rates by $K$ and best single-feature decision rule. Rule: $\det(C) < 0.014 \to$ sign reversal (accuracy 91.3\%, sensitivity 85.7\%, specificity 100\%). An AND rule ($\min_k C_{kk} < 0.55$ and mean entropy $> 0.40$) achieves 95.7\% accuracy with 100\% sensitivity.}
\label{tab:sign_determinants}
\begin{tabular}{lcc}
\toprule
$K$ & $n$ & Sign reversal rate \\
\midrule
2 & 7 & 0/7 (0\%) \\
3 & 8 & 7/8 (87.5\%) \\
10 & 8 & 7/8 (87.5\%) \\
\bottomrule
\end{tabular}
\end{table}

Binary matrices ($K{=}2$) never produce exposure sign reversal, consistent with classical attenuation theory. For $K \geq 3$, the determinant of $C$ is the strongest single predictor: $\det(C) < 0.014$ identifies sign-reversing matrices with 100\% specificity (zero false positives) and 85.7\% sensitivity. An AND rule combining $\min_k C_{kk} < 0.55$ with mean column entropy $> 0.40$ achieves 100\% sensitivity at 88.9\% specificity. These rules are robust to $\beta$-scaling (accuracy unchanged at $0.3{\times}$ and $3{\times}$ effect magnitude) but sensitive to $\beta$-sign structure: under uniform-positive $\beta$ (all coefficients same sign), no matrices produce sign reversal regardless of $C$, as cancellation across differently-signed coefficients is the underlying mechanism.

\subsection{Y-Stratified Non-Differential Verification}
\label{sec:y_stratified_test}

We directly test the non-differential assumption (Definition~\ref{def:nondiff}) by stratifying confusion matrices by outcome-proxy variables and testing $C(Y{=}\text{high}) \approx C(Y{=}\text{low})$. Using 12 LLM configurations on VAST ($K{=}3$; 4 models $\times$ 3 prompting strategies, 1{,}500 samples each), we compute per-stratum confusion matrices under three Y-proxies and apply permutation tests ($B{=}10{,}000$, test statistic: $\max_{j,k} |C^{(0)}_{jk} - C^{(1)}_{jk}|$).

\begin{table}[h]
\centering
\small
\caption{Y-stratified non-differential test. Content-independent Y (text length) shows no significant violation (1/12 rejection $\approx$ expected false positive rate at $\alpha{=}0.05$). Content-dependent proxies (topic, sentiment) show expected violations since LLMs observe text content.}
\label{tab:y_stratified}
\begin{tabular}{lccc}
\toprule
\textbf{Y proxy} & \textbf{Type} & \textbf{Reject ($\alpha{=}0.05$)} & \textbf{Median $p$} \\
\midrule
Text length & Content-indep. & 1/12 & 0.490 \\
Topic & Content-dep. & 11/12 & 0.002 \\
Sentiment & Content-dep. & 10/12 & 0.017 \\
\bottomrule
\end{tabular}
\end{table}

The single text-length rejection (Llama-4 few-shot-3, $p{=}0.027$) is consistent with the expected false positive rate ($0.05 \times 12 = 0.6$). Content-dependent proxies (topic, sentiment) show systematic rejection because LLMs process text content from which these variables can be inferred, violating $Y \perp \text{text} \mid A$. In causal inference applications, $Y$ is typically an external outcome (e.g., health status, economic indicator) that cannot be inferred from the annotated text, satisfying the independence condition. The content-dependent rejections confirm statistical power: the test can detect violations when they exist.

\section{Notation}
\label{app:notation_table}

\begin{table}[h]
\centering
\small
\caption{Key notation used throughout the paper.}
\label{tab:notation}
\begin{tabular}{cl}
\toprule
\textbf{Symbol} & \textbf{Description} \\
\midrule
$K$ & Number of annotation categories \\
$C$ & Column-stochastic confusion matrix, $C_{jk} = P(A^*{=}j \mid A{=}k)$ \\
$A$, $A^*$ & True and observed (misclassified) annotation \\
$T$ & Treatment variable \\
$Y$ & Outcome variable \\
$\tau$ & Causal effect of interest \\
$\delta$ & ASV perturbation parameter, $C(\delta) = (1{-}\delta)I + \delta C_0$ \\
$\delta^*$ & Critical ASV value (magnitude/significance/sign-flip) \\
$B(C)$ & Bias function, $= \operatorname{plim}(\hat{\tau}) - \tau$ \\
$\beta_k$ & Treatment effect coefficient for category $k$ \\
$D_k$ & Dummy variable for category $k$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Use of AI Assistants}
\label{app:ai-assistants}

We used Claude (Anthropic) as an AI coding and writing assistant throughout this work. Specifically:
\begin{itemize}
    \item \textbf{Code:} drafting experiment scripts, data-processing pipelines, and analysis utilities; debugging and refactoring.
    \item \textbf{Writing:} drafting and revising paper sections, polishing prose, and reformatting \LaTeX{}.
    \item \textbf{Literature:} summarizing related papers to inform the related-work section.
\end{itemize}
